# Atom 当前需求与缺陷总账

**更新时间：** 2026-09-04
**权威远程基线：** `main@f2d2fd0`；A模式改构前回退标签`pre-a-mode-consolidation-20260904`
**用途：** 本页是当前唯一的需求排队与状态裁定入口。规格负责产品定论，专项计划负责实施细节；旧计划中的历史勾选、聊天记录、Agent 自报和 Atom 世界均不能替代本页状态。

## 会话回查证据

- **本机原始记录**：已回查`C:\Users\worker\.codex\sessions\2026\08\31\rollout-2026-08-31T23-06-14-01a0585b-4fab-79c3-b626-ab9d8c6eaa75.jsonl`，共31,010条JSONL记录；剔除环境、插件、目标和系统注入后，实际用户消息168条，时间覆盖2026-08-31 16:27:55至2026-09-03 15:19:22。
- **使用边界**：原始会话只用于找回用户定论、现场复现与授权线索；当前状态仍须与Git、测试、部署回读及当前Graph事实交叉验证，不能把聊天中的旧方案或Agent自报直接当成完成事实。
- **恢复结论**：软件重开丢失的是界面可见历史，不是本机原始会话。下列总账已经按回查结果补入明确遗漏、延后项、冲突项与证据缺口。

## 状态与证据

- **✅ 已关闭**：代码、聚焦测试、部署回读及适用的真实用户链证据齐备。
- **🟢 已交付待观察**：代码、测试和部署证据齐备，但仍接受下一次真实使用反馈；不得因缺用户重复操作而重开开发，也不得声称用户已现场确认。
- **🟠 未解决**：用户结果尚未达到，或当前仍有复现证据；进入执行队列。
- **⏸ 低优先排队**：需求有效且必须实施；只是不抢占更高优先级时间窗。前项完成或其依赖被局部阻塞后，继续执行本项，不得解释为取消、不开发或无限搁置。
- **🚫 已撤回／已替代**：后续定论已明确取消原方案；不得重新开发旧方案。

证据按“代码提交 → 聚焦测试 → 部署回读 → 用户现场结果”逐层记录。缺少后一层不能抹掉前一层，也不能把前一层冒充后一层。

## 当前执行队列

- **P0｜🟠 ESG测试域创建Program超时（当前首要）**：现场任务在`🧊manage/包办/判重/ESG计划/test`以`--agent 🧊manage`连续创建最小`thing@program`均在15秒返回`ATOM_INTERACTION_TIMEOUT`，随后精确回读均为`ATOM_NOT_FOUND`，确认未发生半提交。首次Situation为`return work_order_catalog({"template":"work-order","version":1})`；排除目录函数后，仅`return True`的全新Program仍同样超时；同域普通14节点Graph约6秒创建并可回读，故当前证据把故障收束在Program创建链，而非`work_order_catalog`业务函数或Graph通用写入。反馈记录`f4607603-26d3-49a6-844c-b9db9ee23d87`；按`systematic-debugging → test-driven-development → verification-before-completion`定位并修复，不以增加超时替代根因处理，不改Graph本体定论。
- **P0｜✅ 账本校准**：已回查168条实际用户消息，消除旧计划、恢复入口、自动化与当前代码之间的已知状态矛盾；后续转为持续收件和证据校准机制。
- **P0｜✅ 开发连续性规则归位**：2026-09-03 00:00—09:00暴露的优先级漂移、进展未即时入账、连续等待及重复全量，已裁定为通用开发纪律。工作区`D:\Project\〇\AGENTS.md`只补充当前目标、即时入账、验证升级和阻塞定界四项，并明确不得删减或绕过原Superpowers技能；Atom独立门禁、脚本和第二状态源方案已撤回，决策记录见[`../specs/2026-09-04-atom-development-continuity-gates-design.md`](../specs/2026-09-04-atom-development-continuity-gates-design.md)。
- **P1｜🚫 工务2—4旧终态已被替代**：CLI精确回读确认旧`推进`整树已位于仍可使用的`外务/旧版本`，且`外务`不再直接包含`推进`。随后提交回执`legacy-b67250821108fc80e9b3f88b38ed55474a83494cc31a0229563837585d79e540`又明确执行`职务→职务·FDE`、`规划→设计`、`实施→开发`，后续ESG现行结构持续建立并运行于`职务·FDE`下。因此旧`工务/规划/实施`目标已被较新的世界演进取代，不得重新创建或反向改名；本项无需Graph写入。
- **P2｜✅ 每小时巡守已校准**：`atom-2`“Atom 目标巡守”为`ACTIVE`、每小时一次；提示已改为先读本总账、默认最小受影响链、即时入账，并优先处理“🔥🔥🔥ESG计划_按逻辑_xlsx”暴露的生产急用功能故障；现场反馈不授权擅改Graph本体与内核定论。
- **P3｜🟠 手机正式域名终端断点**：2026-09-04最小链回读为：4784 `200/约193ms`，4785按预期拒绝无Tailscale身份的直连请求，Serve仍代理至4785，电脑绕过测试代理访问`https://worker.tail33a2eb.ts.net/`为`200/约250ms`；但Tailscale状态显示手机`pixel-10a`离线。当前断点位于手机tailnet连接而非Atom代码，须先恢复手机Tailscale在线再做同域名浏览器验收；用户当前以ToDesk远程使用，故不阻塞其他功能。
- **P4｜🟠 A 模式收束（被P0局部抢占，未取消）**：用户已批准交互定论：团上右键单击普通向内剖开，同一团右键双击沉浸向内剖开，当前团空白处右键单击只返回一层且非沉浸；右键连击间隔默认`420ms`、可调`240–800ms`。S、D退出运行路径，F融入A沉浸，A键不作为退出旁路。必须使用“向内剖开”，不得写成“向外展开”。开发前远程回退点已固定为`pre-a-mode-consolidation-20260904`；规格与计划已提交，隔离分支`feat/a-mode-consolidation`的Task 1已在`b1bff98`以聚焦测试`31/31 PASS`收口。P0修复后从该提交继续。
- **P5｜⏸ 零 Agent 创世（后续排队）**：当前世界已有合法 Agent，因此困难度和紧急度低于A模式收束；保留为改变信任根的独立后续规格，待P4完成或局部受阻后继续，不得视为取消。

当前P0只指上述Program创建与交互回执故障；其余新的即时反馈先进入下方矩阵，不因到达时间自动插队。

### 当前P0补充证据与目标同步

- **恢复历史GREEN及排除结论（08时最新）**：隔离候选普通读写历史加载从2次降到0；最小链35/35通过，覆盖真实事务、丢弃、重启后从中央日志恢复以及冲突不写入。真实规模只读副本`C:/Users/worker/AppData/Local/Temp/atom-real-write-acceptance-XKvkiQ`：创建4366ms、写后读取4393ms，回滚、重启、源文件未改全部通过。读取相比原4.6秒无明显改善，故历史克隆不是主瓶颈，不能声称关闭。仅保留确定的按需加载优化，移除本轮临时细分计时。生产仍为61343ca；下一步继续定位engine内Program初始周期之后至Explore完成之前的剩余约3.5秒，不重复已排除的授权筛选／初始化／历史加载假设。

- **恢复历史按需加载RED（2026-09-04 08时巡守）**：上一轮分段测量query授权161ms、边界5ms、描述1ms，候选／授权运行时初始化仅数毫秒，不能解释写后读4.6秒。源码确认legacy入口每个交互无条件加载并克隆事务Transform历史，实际只有恢复`.rst.`两个分支使用。真实服务＋中央持久化最小测试中普通Explore与改名成功，但历史加载2次（应0），取得RED。当前隔离候选改为每交互懒加载，保留恢复、鉴权与中央事务；尚待GREEN与真实规模验收，不声明延迟已解决。

- **性能改善已部署（当前最新）**：隔离`7f8e825/d2d4807`已合入`main@20e91e2/61343ca`；锁权限57/57、指纹投影32/32、候选授权及回执25/25、合入最小链3/3通过。真实规模隔离副本创建4093ms、立即读取4623ms、Program收尾6902ms，回滚／重启／源文件未改通过；副本`C:/Users/worker/AppData/Local/Temp/atom-real-write-acceptance-XDmG3z`保留。4784受控重启后health revision7306投影published，生产候选精确CLI回读569ms、世界修订仍`4cd026...7f7e`。热态读已正常；写后首次读取4.6秒仍保留优化项，不能用热态569ms冒充写后延迟。尚未推送。
- **Program指纹RED／GREEN**：同一已验证不可变世界连续两次投影校验仍重复序列化并哈希Program语义（RED=2次，应为0）。候选仅对运行时自行创建并深冻结的records及同一Program对象缓存原始完整指纹，key保留Program次序、Agent身份位与隔离模式，每快照最多128条且WeakMap随快照释放；不改变哈希内容或持久格式。32/32投影生命周期回归通过，源码、结构、Agent权限和模式变化仍失效。正在真实规模副本验证，未部署。
- **慢读当前断点（2026-09-04巡守）**：隔离分支`7f8e825`仅保存动作分类复用，权限矩阵57/57通过，尚未合入或部署；真实副本读取7609ms，仍未达标。一次CPU采样保留于`C:/Users/worker/AppData/Local/Temp/atom-read-profile-8de412e704744c2686a209fdffc52073`：`programSetFingerprint`多处调用各耗约1.8秒，明显高于单次锁扫描；还含脚本复制／回滚／启动的journal解析耗时，不能把整个采样等同单次读耗时。下一步限定分析不可变records上重复Program语义指纹计算及可安全复用边界，先RED再修复；不得削减指纹字段、权限校验或业务语义。生产仍运行`27c9211`回执修复，未重新全量验证。
- **慢读假设排除与新RED**：调度器参数候选在真实副本仍读取8506ms；进一步确认4784的resolveServerAgentContext本就传入共享调度器，因此撤回该候选代码与测试，不作为性能修复。新的确定性回归显示20次读对100条仅写锁重复读取动作4000次；按当前访问控制器缓存动作分类后GREEN，权限矩阵57/57通过，真实规模延迟复测中。仅复用分类，不跳过路径／标签／Program鉴权。
- **首次读取慢的复用缺口**：组合入口调用`resolveAgentContext`未传入启动时已有programScheduler，缓存失效后因此新建调度器并重复全世界Program声明检查。已取得最小RED，传入既有调度器后解析缓存／创建Agent／重配授权4/4通过；正在真实规模副本复测，未部署本项。此修复复用精确源码缓存，不跳过变更声明和权限校验。
- **回执修复已部署（最新）**：隔离提交`23cfba6`已合入`main@27c9211`，合入最小回归1/1通过；受控重启4784后health为revision7305、投影published，公开CLI成功精确回读`test/工单执行器候选`，源码与现场所述一致。真实规模隔离写入回执已通过，生产本轮只读验证，未重放或改写三单。提交回执修复已交付待观察；约8.3秒的首次读取及10.4秒派生收尾性能仍未关闭。当前未推送新提交。
- **回执候选隔离验收**：生产同款投影仓库首次暴露提前回执后读到旧resolutionManifest（RED）；在组合入口先刷新修订清单再通知回执后，真实规模副本端口51432创建Program`4076ms`、立即读取`8302ms`成功，无修订错误；回滚、重启53648和源文件逐字节未变均通过，副本`C:/Users/worker/AppData/Local/Temp/atom-real-write-acceptance-0BAPdc`保留。Program派生收尾仍约10.4秒，因此读取性能仍未关闭。受影响链44/44、投影／组合58/58、HTTP独立交互及Web通知5/5通过；准备阶段提交及部署，尚不宣称生产闭环。
- **回执P0最小链进展**：真实引擎＋中央持久化回归复现：事实已落盘，但提交后Program投影被阻塞时`onCommitted`尚未触发（RED）。隔离分支将完整回执组装与派生投影收尾分开，交互运行时贯通一次性通知，最小受影响链`44/44 PASS`。尚未部署；正在以生产世界只读副本、随机端口和生产同款Program投影仓库验证，不重复全量。ESG现场另报三单已原生审核通过并read-back为valid=true，属于现场报告，不代表回执故障关闭。
- **审核参数文档缺口**：现场报告三单已原生提交为`待验收`、`missing=[]`（尚未独立复核）。源码确认`submit.decision`仅可省略或为`通过`；通过时`reviewer`与`reviewed_at`必须非空，提交时间可复用既存值，函数自身写入`Criteria.验收.审核`并转`已通过`，无需先fill审核。在待验收状态fill本身不允许。通过路径会把审核意见设为`[]`，已有业务缺口应继续保存在成果而非被掩盖。注册表只列可选字段、未公开条件必填及枚举，须补齐公开合同；软件状态不代表正式管理审批。
- **现场映射澄清**：ESG任务明确不请求改变内核校验；业务资料缺口属于其如实交付的成果，继续完整保存在成果Graph及实际产出引用中，`Step.操作.异常`仅记录未解决执行异常。仅在加工实际完成且确无未解决执行异常时填`[]`。这是现场业务映射说明，不冒充平台已公开的语义合同；后续文档需明确两者边界，软件valid也不代表业务资料已补齐或已获正式验收。
- **工单Step校验缺口（源码已核实）**：现场三单validate仅报`/Step`，read-back显示状态`已完成`、实际产出非空、异常为真实缺口。`program-worker.py`的`incomplete_work_order_groups`对当前Step要求实际产出非空、状态恰为`已完成`且异常为空；因此异常非空足以解释拒绝，不能要求现场擦除真实异常过关。当前guidance只说记录异常，missing只列组路径，未解释该拒绝条件，属公开合同／诊断缺口，跟随回执P0补齐；未授权放宽业务校验，也不得将源码现状冒充用户已定论。
- **最新现场回报（待独立复核）**：ESG任务报告修订错误已消失，精确回读候选为`@program`，最小`.run`成功；原生创建ACT010及fill均15秒超时，但随后精确回读分别确认已创建、`filled=true`且状态执行中，未重放。当前优先定位提交后回执超时；不得因修订错误暂时消失宣布整体关闭，也不据此要求业务重复写入。
- **部署后现场复现（当前首要）**：修复部署后，已有普通`test/工单执行器候选`执行一次`.typ.program`仍15秒超时，立即回读报`GRAPH_COMPATIBILITY_MANIFEST_REVISION_MISMATCH`；现场未重放。创建修复不能据此判定整体关闭；当前先只读核对目标类型、权威修订和事务／兼容清单，再修复回读一致性与类型转换路径，不覆盖或回滚业务数据。普通三单12步骤已保存。
- **最新部署状态（覆盖下文历史未部署）**：创建链重复声明检查修复已合入`main@5916883`，隔离验收脚本为`69f5838`。最终候选一次全量`1704/1704 PASS`、合入后最小链`8/8 PASS`；2026-09-04 03:18按`Atom Graph Runtime`任务受控重启，PID由44932变为46140，health revision7289、投影published，公开CLI成功回读ESG test域。生产Program创建待现场调用，不能把隔离成功替代现场结果；普通批量提交后回执超时仍未关闭，当前继续处理。
- **回信限制**：平台安全审查两次拒绝向已核实标题的ESG任务发送部署进度，要求更明确的用户披露授权；未绕过。该限制只影响跨任务通知，不影响本地修复、部署和总账；需要用户允许发送仅含服务恢复／可验收状态的通知。
- **调试进展**：隔离工作树`.worktrees/esg-program-create-timeout`、分支`fix/esg-program-create-timeout`；原有创建／候选权限基线`20/20 PASS`。真实Python检查计数复现：只新增1个普通Program，候选前后声明派生却启动8次检查（应只检查新增1次）。已按TDD取得RED，并以精确路径＋源码缓存纯AST声明结果后取得`8/8 GREEN`；变更源码仍重验、非法声明仍拒绝、候选缓存独立。此为已证实的重复工作修复，尚不等同生产15秒故障已关闭；下一步候选权限回归、实际规模隔离创建及提交／回执边界验证，未部署。
- **工单合同**：已向ESG现场回传现行`fill.values`三组字段形状；`work_order_catalog`实际不比公开registry额外展开字段，不能作为缺失文档的替代入口。字段文档补齐跟随当前P0之后，不改工单业务含义。
- **阶段提交**：`fix/esg-program-create-timeout@0553b1e`已保存声明检查复用修复；创建、候选授权、拒绝后隔离及声明检查最小链`28/28 PASS`（约24.6秒整套），尚未合入或部署。已明确回告ESG现场：只可在授权test域继续保存普通事实及应用准备，不冒充原生工单生命周期验收，不伪造原生元数据。下一步在实际规模隔离副本验证创建并排查提交／回执边界，再部署；不得从单元GREEN直接认定P0关闭。
- **实际规模验收**：候选在当前生产世界的只读复制副本、随机端口59812上创建最小Program成功`7414ms`、回读`239ms`，无Program失败；中央事务回滚、修订恢复、随机端口重启均通过，生产源文件逐字节未改。证据副本保留`C:/Users/worker/AppData/Local/Temp/atom-real-write-acceptance-GzsgDd`。最终候选已开始唯一一次全量发布验证，未部署；普通批量写入的提交／回执超时仍独立待验，不随创建用例一起关闭。
- **现场补充**：普通`test/工单执行器候选`约5秒创建成功，随后仅`thing.typ.program`同样15秒超时；此前目录调用的准确参数为`{"template":"work-order","version":"1"}`。另一次普通四项Situation批量更新超时后，精确回读确认四项均已提交，因此超时不能一概解释为未提交；禁止盲目重放，须核对提交与回执边界。
- **当前开发目标**：按官方Superpowers持续完成本总账全部未关闭、未撤回的已批准需求与缺陷；低优先排队仍在目标内。每次从最新总账确定当前事项，不在目标正文冻结具体故障及完成状态。依用户显式顺序、必要依赖、紧急度、重要度推进；优先核实并解决“🔥🔥🔥ESG计划_按逻辑_xlsx”的生产急用工单／槽体功能反馈，再接续已批准队列。反馈须对照用户定论和公开合同，不授权重构Graph本体或内核范式。开发前落实已授权推送与可验证回退点，阶段成果验证后提交；急用功能以部署和公共入口回读收口。即时入账、复用同revision证据、不重复全量，不因局部阻塞全局停工，不擅改业务事实。
- **本次提示再校准（2026-09-04）**：已通过产品接口更新既有`atom-2`，保留ACTIVE及每小时频率；移除提示中固定“Program创建／类型转换故障→A模式”的过期快照，改为从本总账动态取最新顺序与状态。明确禁止重做已关闭项、容易事项插队和无依据批量审查。再次查询产品目标仍为blocked，工具清单仅提供创建和完成／阻塞操作，不支持改正文或恢复；本次只更新此处权威目标正文，不虚称产品目标模式已更新。
- **提示词更新（2026-09-04）**：已将`atom-2`每小时巡守从固定“A模式主线”改为读取本总账动态接手，明确ESG生产故障优先、低优先项仍须完成、超时写入先核实提交而不重放、授权推送回退、最小验证及安静巡守。目标栏正文仍受下述接口限制，本段为最新目标文本，不声称产品目标栏已修改。
- **产品目标栏限制**：`get_goal`仍返回旧目标为`blocked`，`create_goal`拒绝覆盖未完成目标；现有API不能修改目标文本或恢复状态。不得为绕过限制虚报完成。上述最新目标为持续执行依据，每小时`atom-2`提示已同步；产品目标栏尚待可用的编辑／恢复入口，不影响开发。

## 核心 Graph 与运行时

- **✅ Slot／Strut 四轴迁移**：`contain/support`已单轨迁移为`slot/strut`，运行时不保留旧轴双读双写；提交链起点`cd67d51`。
- **✅ Agent 子树移动能力**：上级Agent对其授权下级可移动含下级Agent Program的后代子树；下级向上仍越权。移动按路径重写和派生索引重建处理，不再误判为逐个Agent重配置；证据`57230c0`及现行授权回归。当前回读同时证明旧`推进`移动已经完成；后续FDE命名是独立且较新的世界演进，不反向改变本能力结论。
- **✅ Slot 相邻层级信号**：`slot({"to":"up|down","labels":[...]})`、接收方`trigger("slot",...)`及 invocation-local `signal()`已部署；不新增`climb`轴。
- **✅ 交互原子隔离**：Explore不再等待未完成写交互；每次交互独立截止，候选计算并行，仅权威提交短临界区串行；`3111dcb`、`d1b7b6c`、`ece6293`、`ead30e2`。
- **✅ 4784启动热态**：本机启动4784时即准备全局投影与索引，不再等首个CLI／Web局部请求临时冷加载；`c03e2f0`、`6340fda`、`ffb42de`，真实热态回读约256ms。
- **✅ Strut接收单轨**：Graph后项唯一决定接收Program；后项以`trigger("strut", {}, main)`自主响应，旧`nodes`已从代码和生产ESG四条链迁出；`970bd0a`、生产迁移回执及重复预检修复`b36cde8`。

## Program、点击与业务推进

- **✅ Strut内嵌判定**：条件Program属于该条Strut的`if.program`，读取复合前项事实与规范化Transform动作信封并返回strict bool；不是节点全局触发器。
- **✅ 通用Transform动作**：CLI/Web统一用Transform登记`$`动作；`$click`只是动作族首例。输入层累计同一exact Thing的无上限点击次数，三点击由Strut判定Program表达，未硬编码成独立功能。
- **✅ ESG测试链**：隔离世界已跑通五阶段连续解锁和四次迁窗；真实世界初态与四条接棒Program已部署。软件验收不等待真实业务节点完成。
- **🚫 新旧值内核判断**：用户已裁定是否发起Transform属于业务使用方；内核不新增旧值、新值或`once_per_revision`判断。
- **🚫 外部节点判定Program**：旧“独立thing@program负责Strut条件”方案已由`if.program`替代，不得恢复。

## Web、游走与编辑

- **🟢 Web/CLI即时一致性**：`88d36c6`修复SSE断线期间漏过CLI／Program revision后仍展示旧局部事实；重连只补拉当前路径与展开路径，并原子导入同一revision。代码、测试和4784部署已证实，后续真实复现须保留网络时序后再重开，不能笼统回退为轮询或全量刷新。
- **✅ Shortcut**：深层激活、坏链保持、渐进路线真实浏览器`5/5 PASS`；人工与Agent用`thing.lnk.EXACT_TARGET`按语义路径改向，Web不要求记ID，也不显示合同JSON；`a48f33f`。
- **✅ ASDF既有缺陷**：双击采用最具体可见节点、F真实owner域径、Strut端点贴边、CLI revision场景连续性均已有聚焦测试和部署证据。
  - **精确命中**：A模式展开后双击内层节点不再误中外部上级；密集小节点点击使用最具体可见目标。
  - **域径返回**：F／右键进入深层域时路径、数据和返回栈使用真实owner链，不再伪装成`manage`直接下级。
  - **场景连续**：CLI造成节点移动、增删改后，Web重建受影响局部场景，不再把布局挤成杂乱重叠图。
  - **关系贴边**：ASDF推支线端点锚定可见边界，不再悬空或穿入节点中心。
- **✅ 设置入口**：星轨统一入口、居中模态窗口、映射分区、CapsLock默认详情模式及模态键盘隔离已集成部署；`3abba9b`、`942284c`、`910e108`。
- **✅ 内部槽位标识隔离**：`@slot-role-*`／`@slot-revision-*`只保留为稳定结构身份，不再投影成普通名称；生产投影命中为0，`93200d3`。

## 延后架构与现场入口

- **⏸ A/F收束（已排到）**：最终只保留A的Slot内嵌视角；右键单击为普通向内剖开，双击为沉浸向内剖开，沉浸只隐藏所进入团之外的信息。手机入口的终端依赖当前受阻，本项进入实施前设计收口。
- **🟠 手机统一入口**：正式域名不改成易变IP；ToDesk只是当前可用替代路径，不构成Atom手机入口验收。服务端链已验证正常，当前手机`pixel-10a`处于Tailscale离线状态；下一步先恢复手机tailnet连接，再确认手机浏览器请求是否到达Serve边界，不重新猜地址或擅自启用公网Funnel。
- **⏸ 零Agent创世（低优先排队）**：保留“零Agent世界→一次性受控创世→旁路永久关闭”的未来边界；排在A模式收束之后，不抢占当前时间窗但仍须后续实施。
- **🚫 `旧版本`等同冻结**：显示名“旧版本”不自动代表备份停用；只有显式默认备份仓语义才停用Program。普通旧版本域仍可参与使用。
- **🚫 工务2—4旧终态**：迁移前备份`C:\Users\worker\AppData\Local\AtomGraph\backups\work-framework-20260901-003435`仍存在；现行CLI回读与后续Graph提交回执共同证明该旧终态已由`职务·FDE/设计/开发`替代，不再列为待办，也不据此回改现行世界。

## 持续开发约束

- **唯一框架**：Superpowers独占规格、计划、TDD、调试、验证和完成裁定；Ponytail仅保留两个最小化检查点，不安装常驻Hook或第二套框架；OpenSpec只作历史资料。
- **最小受影响链**：默认运行当前变更能触达的最小测试链；仅在发布门槛、共享主干高风险变化或专项计划明确要求时运行全量。禁止反复全量审查消耗额度。
- **即时收件**：用户反馈先写入所属规格／计划并标记状态、优先级与证据缺口；一般新反馈不自动打断更高优先级事项，但“🔥🔥🔥ESG计划_按逻辑_xlsx”中经复现的已批准功能生产故障作为用户显式特例优先修复。所有收件均不依赖聊天上下文。
- **ESG现场试跑边界**：Session“🔥🔥🔥ESG计划_按逻辑_xlsx”负责使用现行能力试跑工单／槽体，其需求、Bug与CLI提交是现场证据而非内核设计权威。所有反馈先与用户定论、产品定位和现行Superpowers规格核对；不得据此擅改Graph本体、权限、Slot／Strut或调度框架。若已批准功能按正确合同仍不能工作，则以该复现进入系统调试与TDD并修复真实缺陷，不得借稳定内核之名回避。
- **安全提交**：阶段性代码在最小验证后提交；推送必须有用户授权。私密世界备份在仓库外保存，软件代码与规范只推`main`。
- **授权收束**：同一类必要权限应一次性、合理范围申请，不以重复申请泛滥中断任务；但推送、外部写入和破坏性操作仍遵守明确授权边界。
- **巡守规则**：用户最终定论为每小时巡守；只在存在未解决事项且能由既有定论可靠推出时继续实施。无变化保持安静，不做重复全量审查；自动化已恢复为`ACTIVE`并改用本总账，后续状态变化仍须如实回写。
- **状态校准**：发现旧计划未勾选、Agent自报、测试或部署与当前代码冲突时，先回查证据并更新本总账，不把不一致当作停工理由。

## 旧计划解释

- 2026-08-31至2026-09-03的专项计划保留设计过程和TDD步骤；其中未勾选框不能越过本总账重新制造待办。
- 已被后续定论替代的计划（例如独立click trigger）保留为历史，不执行其未勾选步骤。
- 专项计划若出现新的当前复现证据，先在本页把对应项从✅／🟢改为🟠并绑定复现，再进入调试或新计划。
