# Memory-Authoritative World and Independent Save Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the running memory world the sole interaction fact owner; reads observe accepted writes immediately while an efficient periodic saver persists versioned recovery points independently.

**Architecture:** One in-process versioned authority accepts validated local transitions and serves all CLI/Web/Program reads. A single-writer saver consumes immutable version snapshots after an interaction-driven quiet window or maximum dirty age, groups pending changes without skipping receipt/history semantics, and advances a durable watermark only after atomic persistence. On restart, the last verified watermark seeds memory; disk never overwrites a newer running version.

**Tech Stack:** Node.js 24 ES modules, built-in `node:test`, existing Atom four-axis engine, world repository and transaction journal.

**Spec:** `docs/superpowers/specs/2026-09-16-memory-authoritative-world-and-independent-save-design.md`

## Global Constraints

- The remote pre-change rollback marker is `backup/pre-memory-authority-20260916` at `86a5e8c8fa4ac074384786d86747432d0f34d111`.
- `atom.json` and private runtime facts are never committed to the code repository or modified for a test.
- Preserve a single public transition boundary, authorization, local conflict rules, one-Transform atomicity, Program source/effect boundaries, stable identities, and rollback evidence.
- Never use an old save or projection to overwrite a newer accepted memory version.
- Record `acceptedRevision`, `savedRevision`, pending/failed save state, and distinct interaction/save timing without exporting user content.
- Do not treat optimistic Web rendering as an authoritative read or a background copy of the existing slow synchronous commit as an efficient saver.

---

## Current execution checkpoint — 2026-09-17

This checkpoint is an index into the detailed evidence below; older RED notes remain historical, not a second current state.

| Work boundary | Current state |
|---|---|
| Memory authority/scheduler, Tasks2/3 | Implemented; integrated lifecycle/capacity obligations continue below |
| Startup/history/compaction, Tasks6/7/8; harness Task13 | Reviewed complete at E2 |
| Owned interaction version Task9; dead-worker recovery Task10 | Reviewed complete through cb99bf8 / 36a5474 respectively |
| History body demotion Task14 | Reviewed complete at031da6f;161 affected tests passed |
| Owned-candidate handoff Task16 | Reviewed complete at0272922; real-copy measurement finished, writes2.40–2.80s and reads~1s remain unacceptable |
| Public shutdown/state Task11 | Reviewed complete through5138159; round1 two findings and round2 snapshot poisoning all addressed |
| Remaining work | Task17 archive/source reuse sole implementation at BASE5138159; Task12 Web translation, Task15 admission and Task18 CLI state visibility queued; strict no-full-hash waiting-chain target remains open |

Task5 final candidate/performance/compatibility/system/full/production/remote acceptance remains open. The0272922 diagnostic proves storage and observed read-during-save independence, not acceptable write latency. Whole-process crash and exact86a5e8c old-code compatibility probes passed on that fixed revision; final-candidate reuse remains required. Production still uses its existing mode. Reuse existing domain agents and exact task briefs; do not repeat successful earlier suites merely for status.

## File Structure

- `src/atom-system/world-runtime/memory-world-authority.mjs`: owns current immutable version, atomic transition ordering, same-version facts/manifest, and accepted receipts.
- `src/atom-system/world-runtime/independent-world-saver.mjs`: owns quiet/max-age scheduling, one save writer, durable watermark, retry/backpressure and close/flush.
- `src/atom-system/adapters/transactional-world-persistence.mjs`: storage adapter for verified recovery and batched, ordered save; existing durable journal/repository remains the storage contract, not a competing runtime fact owner.
- `src/atom-system/adapters/legacy-engine-adapter.mjs`: binds the Engine's existing `commitWorld`/snapshot ports to the memory authority and saver without changing public command syntax.
- `src/atom-system/public/interaction-runtime.mjs` and Web/CLI adapters only if needed: convey accepted versus saved state; no UI-only shortcut.
- `tests/atom-memory-authoritative-interaction.test.mjs`, `tests/atom-world-transaction.test.mjs`, `tests/atom-legacy-runtime-composition.test.mjs`, and targeted CLI/Web tests: RED/GREEN, recovery, compatibility and user journey.

### Task 1: Establish the red contract and cost attribution

**Files:** `tests/atom-memory-authoritative-interaction.test.mjs`, `work-engine/atom-language/engine.mjs`, `src/atom-system/adapters/legacy-engine-adapter.mjs`, current Superpowers ledger.

**Interfaces:** Consumes existing `createLegacyWorldService({execute,transactionProvider})`; produces a repeatable failing test for read-after-write while storage is blocked and timing evidence for currently unlabelled intervals.

- [x] **Step 1: Write the failing test.** Use a gated persistence `commit`, start one Transform, then issue Explore through the same World Service before releasing the gate; assert new value and prompt acknowledgment.
- [x] **Step 2: Run RED.** `node --test --test-isolation=none tests/atom-memory-authoritative-interaction.test.mjs` currently fails with `before !== after` in 0.55 seconds.
- [ ] **Step 3: Attribute the missing timing.** The content-free `security-rebuild` probe is RED→GREEN and its affected 16 tests pass; source notification/snapshot timing and real-scale attribution remain. Compare named stages with total `transform-stage` elapsed before closing this step.
- [ ] **Step 4: Re-run existing baseline.** `node --test --test-isolation=none tests/atom-world-transaction.test.mjs` must remain 102/102 before architectural changes; current valid baseline is 102/102.
- [ ] **Step 5: Commit only the reproducible test and timing diagnosis**, then update the existing ledger with the exact cause and measured stage boundaries.

### Task 2: Build one versioned memory authority

**Files:** Create `src/atom-system/world-runtime/memory-world-authority.mjs`; create `tests/atom-memory-world-authority.test.mjs`.

**Interfaces:** `createMemoryWorldAuthority({initialSnapshot})` produces `snapshot()` and `accept({expectedVersion,expectedRevision,nextSnapshot,receipt})`; a successful `accept` returns `{acceptedVersion,acceptedRevision,savedVersion,savedRevision}`. `markSaved({version,revision})` changes only the durable watermark. The monotonic version guards A→B→A hash cycles; facts and manifest are read together from one version.

- [x] **Step 1: Write RED tests** for immediate read-your-writes, rejected stale revision/version, A→B→A save ordering, older save not rolling back facts, and exclusion of shallow-frozen mutable nested facts. Disjoint rebase remains an integration test at the existing closure-aware coordinator, not a new rule in this core.
- [x] **Step 2: Run** `node --test --test-isolation=none tests/atom-memory-world-authority.test.mjs`; observed initial constructor RED, then A→B→A and shallow-freeze RED before each fix.
- [x] **Step 3: Implement** the smallest authority around existing sealed revision utilities; atomic publication assigns one snapshot with facts, manifest, content revision and monotonic version. Readers take that object without storage I/O.
- [x] **Step 4: Run the new tests and existing local conflict tests** in `tests/atom-world-transaction.test.mjs`; new authority and revision tests `10/10 PASS`, existing transaction/recovery suite `102/102 PASS` at this revision.
- [x] **Step 5: Commit the authority and its tests.** Saved as `a5eaea5` on the isolated branch.

### Task 3: Build the independent, bounded saver

**Files:** Create `src/atom-system/world-runtime/independent-world-saver.mjs`; create `tests/atom-independent-world-saver.test.mjs`.

**Interfaces:** `createIndependentWorldSaver({save,quietMs,maxDirtyMs,clock,onState})` exposes `enqueue(version)`, `status()`, `flush()` and `close()`. `save(version)` persists a stable immutable version and returns its verified revision. Only one save call may run per world; a dirty version newer than the completed save is scheduled again.

- [x] **Step 1: Write RED tests** for quiet-window coalescing, maximum dirty age under continuous input, single active save with 100 queued accepts coalescing to the latest, retry after failure, and close flush.
- [x] **Step 2: Run** `node --test --test-isolation=none tests/atom-independent-world-saver.test.mjs`; three original tests failed on missing implementation before GREEN.
- [x] **Step 3: Implement** bounded scheduling and state changes; disk I/O stays behind injected `save`, with no memory authority lock while awaiting it. The real `save` adapter still must keep CPU serialization off the interaction event loop.
- [x] **Step 4: Run the new suite** with gated/failing save and deterministic clock; `4/4 PASS`. This proves orchestration only, not nonblocking real I/O.
- [x] **Step 5: Commit the scheduler and tests.** Saved as `a43c6f3` on the isolated branch.

### Task 4: Integrate real transactions, journal evidence and restart

**Files:** Modify `src/atom-system/adapters/legacy-engine-adapter.mjs`, `src/atom-system/adapters/transactional-world-persistence.mjs`, and only the necessary `world-runtime/commit-coordinator.mjs` / repository boundaries; extend `tests/atom-memory-authoritative-interaction.test.mjs` and `tests/atom-world-transaction.test.mjs`.

**Interfaces:** Engine `commitWorld(transition)` returns an accepted-memory receipt without awaiting saving; `readCommittedSnapshot` returns the current memory version; storage `saveThrough(version)` verifies ordered durable receipts and a recovery watermark. Existing `programExecutionForInteraction`, `recordProgramExecution`, restore evidence and rollback must read accepted in-memory history while running, with durable save snapshots preserving restart semantics.

- [ ] **Step 1: Extend RED** to real fixture storage: blocked save plus immediate Explore/ack and injected EIO plus readable dirty status are GREEN (`2/2`); two independent edits and interrupted-process recovery are still required.
- [x] **Step 1a: Preserve the coordinator rules at the new boundary.** An in-memory world/journal port now runs the existing coordinator and exposes accepted facts, receipt and pending Program outcome together; focused tests `3/3 PASS`. It is not yet the public runtime or a durable save adapter.
- [x] **Step 1b: Establish an opt-in real vertical slice.** The World Service can opt in to memory authority. A real Atom Transform is accepted and read before save; a worker persists ordered transactions and Program outcome events, then Graph projection, and the durable repository reads the saved revision. Blocked-save and EIO recovery were exercised through the public World Service. Focused integration/port/worker tests `10/10 PASS`; existing affected transaction/Transform/Program suites `165/165 PASS`. Production defaults and CLI/Web/Program deployment remain unchanged.
- [x] **Step 1c: Close recovery and backup ordering gaps.** Coordinator recovery is idempotent after prepare/world-write faults, Program source plus subsequent outcomes replay in order, failed close releases the writer, and HTTP Transform/Explore reads accepted memory before save. Private backup schedules only after the durable watermark advances; shutdown flushes the final backup before closing its trigger. Affected memory/HTTP suites `50/50 PASS` on 2026-09-16. This is still opt-in and not yet a production deployment.
- [x] **Step 1d: Remove per-request full-history cloning.** On the 14.7MB private-world copy, `committed-snapshot` fell from 2.26s to 0.09s and initial `transform-log` from 1.88s to 0.002s by reading the latest receipt/log records directly from in-memory history. Copy-only acceptance (Transform→Explore→close→rollback→restart) passed; write 8.17s→4.08s, read 3.02s→2.35s. Remaining 0.92s commit, 2.36s Program projection, 1.1s Explore engine and 3.06s max event-loop delay are not yet acceptable final latency. Cold recovery remains ~65s due 736MB/940MB append logs.
- [x] **Step 1e: Expose an explicit deployment switch and restart boundary.** `--memory-authoritative` is parsed by the supervised Graph entry; default disk behavior is preserved until exact deployment. Closing a memory service unregisters its closed writer so a same-process restart on the same world can accept a new Transform. The HTTP restart test is GREEN; the new switch had RED→GREEN. Production has not been switched.
- [x] **Step 1f: Isolate copied-world private backups.** The inherited official backup destination was accidentally used during a system test with a copied world. Tests were stopped; the official world remains unmodified, but the private backup repository's newest local commit contains test data. A path-scoped selection rule now refuses the inherited destination for non-official worlds; copied-world RED→GREEN and a backup-disabled `test:system` `331/331 PASS`. Restoration of the private repository's newest snapshot from the official world requires the user's exact remote authorization after an auto-review rejection; prior Git backup commits remain recoverable.
- [ ] **Step 1g: Resolve real-scale durable-writer failure.** A later fresh copy still exhausts the worker heap or takes several minutes replaying ~780MB world and ~966MB journal history during one save. The latest-receipt fast path avoids an extra clone but is insufficient. Existing Windows fallback compaction does not prune this log without a durable directory-sync proof. Keep production switch off until a tested incremental/segmented persistence path retains exact recovery evidence at this scale; don't mislabel opt-in fixture GREEN as production-ready.
  - **Measured boundary, 2026-09-16:** On isolated copy `DvN5OP`, a standalone `createJsonWorldRepository(...).read()` took 43.45s and retained 3.21GB heap (4.33GB RSS); a separate `createJsonTransactionJournal(...).latestReceipt()` took 12.30s and retained 1.20GB heap (1.39GB RSS). The worker invokes both adapters in one heap, so the 4GB worker OOM follows from duplicated full-history materialization before a new record is saved. This is a storage-owner design failure, not a need to raise the heap limit. The copy was read only; production switch remains off.
  - **Local mitigation, not acceptance:** Removed the second full parse of already-verified world-log frames. The same copy's standalone world read dropped to 25.48s / 2.18GB heap (from 43.45s / 3.21GB); the existing transaction suite remained 102/102 green. A no-op save through the real worker on this isolated copy completed in 40.53s without OOM. This does not demonstrate an efficient changed-world save or recovery and does not close Step 1g; incremental/segmented persistence remains required.
  - **Changed-world acceptance still RED:** A fresh-copy `--memory-authoritative` end-to-end acceptance produced no completion after several minutes and was manually interrupted; no success or OOM result is claimed. The official source and production service were untouched. The duplicated-parse mitigation is insufficient for the required bounded save latency; stop repeating full-copy acceptance until the old-log replay boundary is removed.
  - **2026-09-17 evidence correction:** Source inspection found `initialReceiptCount` computed against the empty temporary journal BEFORE copying source journals. It is therefore zero and the later rollback selects all historical receipts. The unbounded acceptance run cannot be attributed solely to saving; prior per-adapter timing/heap and worker OOM evidence remain valid. Fix the acceptance oracle before re-running the real journey.
- [ ] **Step 2: Implement one startup owner** seeded from the existing verified committed view and bind all runtime reads to it. No request may reload a stale disk snapshot into active memory.
- [ ] **Step 3: Move commit I/O behind `saveThrough`** and batch or append only the changed closed set plus ordered receipt/history metadata; keep existing private recovery and integrity checks. Measure bytes and stage timing to prove no per-interaction full-world rewrite or per-cycle full-history scan.
- [ ] **Step 3a: Keep CPU off the interaction loop.** The real save adapter must serialize/hash/compress the changed closed set in a worker or equivalent bounded off-thread lane, with a real-scale event-loop-delay assertion while saving; an unawaited Promise on the same loop is not sufficient.
- [ ] **Step 4: Run the RED fixture, 102 transaction tests, Program source/effect and restore tests**, then targeted CLI/Web tests; resolve semantics conflicts at the owning boundary, not by weakening assertions.
- [ ] **Step 5: Commit the integrated runtime.**

### Task 5: Public state, exact rollback and production acceptance

**Files:** Public response/state adapters as required, focused contract tests, existing ledger and plan.

**Interfaces:** Public responses retain existing revision fields and add explicit accepted/saved watermark and save state; shutdown requests flush with a bounded timeout. A saved older version can seed restart but never overwrite a newer live version.

**Recovery acceptance gap audit:** Retained harness agent and root read fixed0272922 tests without rerunning. Existing transaction tests1047+ and1733–1800 cover temporary/short/sync failure, beforeappend, aftersync, beforepublication and compaction crash images; writer-lifecycle118+ covers lostACK replay and wrongwatermark149+, including new writer restoration140–146. Memoryintegration13+ proves accepted-before-save and101+ proves postflush disk, but does not yet prove wholeprocess crash after acceptance and a brand-new memoryprocess recovering only the old saved point. Add only that missing tiny public-service journey: seed durablev1, child accepts v2 with held/long delayed save and reports acceptedv2/savedv1/pending; parent force-terminates that owned child without orderlyclose; a new child reads v1 and no durablev2receipt/status claim. Preserve fixture/disablebackup, bounded child waits. Do not duplicate the already-covered crash matrix. This is a Task5 test obligation, not a new runtime feature; finalcandidate verification still pending.

**Wholeprocess gap diagnostic GREEN on0272922:** Root added only a retained diagnostic driver `C:/Users/worker/AppData/Local/Temp/atom-memory-trace-a5905d22e0d0483381f7e924987f564e/whole-process-crash-probe.mjs`, with source-root argument and15s owned-child limits. Real engine/WorldService savesv1, accepts/readsv2 while savedwatermark remainsv1, then parent terminates only its testchild withSIGKILL withoutclose. Newmemoryprocess recoversv1 facts/revision,accepted=saved=v1,pendingfalse,and no durablev2receipt. Exit0,1.092s, retainedfixture `C:/Users/worker/AppData/Local/Temp/atom-whole-process-crash-kY33zc`. Initial driver attempt failed because it called nonexistentpersistence.latestReceipt; corrected to the existing onAuthoritativeWrite receipt callback before validrun, no productfix involved. Inheritedbackups disabled; no production facts or productsource changed. Reuse this narrow driver against the finalcandidate once, not an expanded duplicatefaultmatrix.

**Exact oldcode compatibility diagnostic GREEN on0272922:** Root prepared detached`../memory-rollback-86a5e8c` at the precise remote rollbackrevision86a5e8c8fa4ac074384786d86747432d0f34d111. Retained `old-code-compatibility-probe.mjs` in the same diagnostic directory saves three synthetic memorytransitions and both completed/failed Program outcomes with0272922,flushes/closes,asserts finalhistoryMode local-patch,then runs a fresh Node importing exactoldpersistence. Oldcode reads currentfacts/revision and correctly bound terminaloutcomes,rolls back only the finalplaintransition via its inversepatch,and reads the expected previousfacts/revision. Exit0,0.757s; fixture `C:/Users/worker/AppData/Local/Temp/atom-old-code-compatibility-ZHvZOq` retained. This is actual oldreader/oldrollback execution,not a field-format inference. Inheritedbackups disabled,no realworld writes. Driver accepts current/oldsource roots for one finalcandidate rerun; production safety still also requires its separate verifiedprivatebackup.

- [ ] **Step 1: Test CLI/Web/Program read-after-write, save pending/failure, same- and disjoint-path concurrent writes, and projection ordering through the real public entry points.**
  - **CLI presentation gap confirmed:** Root read `cli.mjs` output path and retained harness agent ran one pure-memory `runAtomCli` probe: a successful result carrying pending=true, acceptedVersion2/savedVersion1 prints only `{"thing~updated":"Synthetic"}` to stdout and nothing to stderr. Both session and one-shot paths call writeGraphResult; graphResult/writeDiagnostics discard saveState. HTTP state correctness does not close actual CLI state visibility. Task18 below owns the minimal presentation fix; no runtime or production change follows from this probe.
- [ ] **Step 2: Test crash points** before save, during temporary write, after durable write but before watermark publication, and after it; verify only complete saved versions recover and no accepted version is silently called durable.
- [ ] **Step 3: Benchmark** separate p50/p95 for local read/write, save duration, save lag, affected paths, bytes and full-history reads using a private real-scale copy; compare with the pre-change 3.065-second reads and 17.805-second writes.
- [ ] **Step 4: Run gates in order:** smallest affected suites, real critical journeys, required architecture/system checks, one final candidate full suite. Do not upgrade while the current layer is RED.
- [ ] **Step 5: Before deploying, verify private world backup and old-code compatibility/rollback on a copy.** Deploy via the existing supervised runtime, read back the exact public revision and E3 journeys; push the exact code revision, wait for its remote checks to finish, and update the existing ledger with evidence.

### Task 6: Correct the real-copy acceptance boundary (I3/U3/D1/E2)

**Scope:** `scripts/accept-real-world-write-copy.mjs` and narrowly related tests/helpers only. No runtime implementation edits.

- [x] Establish a failing regression using a synthetic source with at least two prior durable commits; the acceptance must preserve those commits and roll back only its own newly accepted commits.
- [x] Capture the copied journal baseline only AFTER copying is complete. Derive the pre-test expected revision from the verified committed world, not a potentially stale physical `atom.json` baseline. Avoid retaining another full historical journal in the acceptance controller; use a short-lived process or existing equivalent boundary.
- [x] Preserve the always-copy isolation rule and source-byte preservation check. Do not add a path that writes directly into the supplied source; never enable cleanup or inherited production backups for validation.
- [x] Add content-free stage start/completion and elapsed-time output so startup, interaction, flush, rollback, and restart can be distinguished. Ensure a stuck stage terminates or clearly reports a bounded diagnostic failure; preserve copied artifacts.
- [x] Run the smallest synthetic regression and the script's directly affected tests. Do not run a real-data/full-system suite; the controller owns that integration run.
- [x] Write a concise report with RED/GREEN, exact files/commands/results and concerns. Commit only assigned files; preserve unrelated dirty repository/plan changes.

### 2026-09-17 coordination and preflight

**Task 6 review, 2026-09-17:** Implementation `44974775b6d5c5e42763381d3eb0610a28386197` passes the synthetic two-durable-commit regression (RED rollback count 3 → GREEN 1), its five script tests and the source-change guard. Independent review found two Important gaps: receipt counting still precedes recovery of copied pending transactions, and the baseline child holds one full journal while a second persistence owner loads it again. Task 6 is not complete. Fix round 1 is queued to the original harness agent after the current runtime implementation; require post-recovery baseline counting without overlapping full-history owners and a pending-recovery regression. The real-scale run remains gated on these fixes. Existing source-byte isolation and retained artifacts must remain.

**Task 6 fix round 1/5 closed:** `8307dddcaa1d7ef2b897ee8e88514357f8929bfa` uses one fresh worker initialization per inspection, obtaining verified revision and ordered metadata only after recovery. Prepared-source-history regression RED rollbackCount 2 / wrong restored revision → GREEN rollbackCount 1 / all three prior source receipts preserved. Affected script tests 6/6, source-change guard 1/1. `/root/harness_review` independently verified both findings ADDRESSED, no new breakage. Root confirms the initialization contract from reviewed Task 7. Task 6: complete (c2b0ecb..4497477 plus scoped fix 7321d47..8307ddd); outer process timeout remains required for CPU-blocked real-copy runs. This is a harness completion, not product performance acceptance.

**Rollback/backup read-only recheck, 2026-09-17:** The annotated remote rollback tag still peels to exact commit `86a5e8c8fa4ac074384786d86747432d0f34d111`. Existing local private backup has independently advanced to `cb35fbe8629a7ec5fa3341a58aaaa69374d58500`; its `runtime-data/atom.json` is 14,688,182 bytes and Git blob `585b6ceb55e76b7189bcb7575e4dbca4bee6e1e3`, exactly matching the current official file's read-only hash. Thus the old statement that the newest local backup contains test facts is superseded; this controller performed no restoration or private-data upload. HTTPS remote backup lookup lacked a noninteractive credential; remote identity remains to be separately verified.

Ruling: Reuse each specialist agent for its own followups, with compact file-based briefs and reports — explicit user preference to minimize repeated context — unrelated tasks still get separate contexts. This plan remains the only product-state record; SDD workspace contains dispatch/review artifacts only. Do not delete workspace/artifacts without user authorization.

| Plan tasks | Shared contract / consistency check | Ruling |
|---|---|---|
| 1, 6 | Timing evidence depends on a correct acceptance baseline | Task 6 precedes the next real-scale run; prior indefinite-run attribution is withdrawn. |
| 2, 3, 4 | Accepted memory versions feed independent durable watermarks | Completed authority/scheduler work is reused; storage fix must preserve ordering, not reimplement the domain transition. |
| 4, 5 | Persistence recovery is prerequisite to production acceptance | Keep production cutover pending until real save/recovery is demonstrated. |
| 4, 6 | Runtime and script both affect observed save time | One runtime specialist analyzes read-only while one harness implementer changes disjoint files. |
| 1 | RED plus timing attribution | Partial evidence, unfinished integration attribution retained. |
| 2 | Immutable memory facts/version tests match implementation | Completed work retained. |
| 3 | Bounded scheduler tests prove scheduling only | Real I/O independence remains Task 4 acceptance. |
| 4 | Efficient incremental persistence vs full-history worker | Existing implementation violates performance intent; architecture specialist supplies the smallest evidence-based correction. |
| 5 | E3 public outcome vs local fixture tests | No local GREEN substitutes for deployment/readback. |
| 6 | Copy-only rollback vs historical baseline | Synthetic history regression required before the expensive copy run. |
| 6, 7 | Harness baseline consumes writer recovery metadata | Use one fresh writer.initialize() per short-lived inspection, then close it; startup payload is not a live query after saving. |
| 7, 8 | Worker owns the same world repository being compacted | Task 8 starts after Task 7 runtime review; cached proof never crosses owners or threads. |
| 7, 9 | Transport reseals current facts; public adapter currently reclones them | Task 9 must preserve verified immutability across the actual adapter/context boundary, not trust Object.freeze alone. |
| 7, 10 | Worker death currently leaves accepted memory readable but no durable retry | Task 10 replaces only the failed disk owner; active memory authority is not reinitialized. |
| 8 | Metadata reuse vs durability proof | Synthetic I/O-count RED plus existing crash/Windows tests; unchanged formats and uncertain-state scan fallback. |
| 9 | Same-version reuse vs stale/mutable caller input | Explicit ownership and version/manifest invalidation tests precede performance claim. |
| 10 | Retry success vs exact durable watermark | Test receipt idempotence and retain unsaved events until acknowledgment identity is verified. |
| 9, 11, 12 | Owned snapshot reused by public replies and Web translators | Snapshot ownership precedes consumer changes; save state is observational, never an authority update. |
| 10, 11 | Replacement worker vs bounded shutdown | One owner handles close; timeout must not create a replacement/retry loop after shutdown. |
| 6, 13 | Sampling adds commands to copy-only rollback scope | Reuse post-recovery receipt baseline; fresh sample IDs, no historical commands selected. |
| 11, 13 | Public response state vs measurement callbacks | Benchmark injects existing service/writer callbacks; does not add a control API or call close-wait pure I/O. |
| 12 | Stale translator source is a source-confirmed suspicion, not yet a test result | Require public RED before implementation, preserve standalone compatibility. |
| 13 | Repeated benchmark vs source safety | Copy-only, backups disabled, bounded samples/outer timeout; synthetic tests precede real run. |

### Task 7: Make the durable worker the sole disk recovery owner (I3/U3/D2/E2)

**Scope:** existing durable writer/worker, transactional persistence, journal metadata export, coordinator historical-chain hydration, and directly affected tests. Preserve the uncommitted single-parse mitigation in the world repository. No new on-disk format or production deployment.

**Controller decision:** The main process currently recovers the entire disk history, then its worker repeats the same recovery. Instead, initialize the existing repositories exactly once in the worker and return the current world plus metadata to the main memory authority. The worker retains full disk history and publication proof state. This removes duplicate ownership without introducing cross-thread verified-seed protocols.

- [x] Add `writer.initialize()` returning verified initial snapshot, ordered receipt metadata `{commandId, historyMode, receipt}`, Program outcomes (including terminal/child-derived outcomes), and compatibility metadata. Journal export must strip historical bodies before cloning and must not call `readState()` then discard already-cloned bodies.
- [x] Add `writer.findCommitted(commandId)` using the same worker journal for on-demand historical rollback/rebase/restore. Keep `save()` usable by existing standalone callers by awaiting lazy initialization. Serialize recovery/save appropriately; no RPC cycle may require waiting on the main interaction.
- [x] In memory mode, initialize exclusively through the writer, then construct the memory ports. Remove main-process disk repository ownership/loading from this branch; default disk mode remains unchanged. Do not silently fall back to disk-mode loading to accommodate old test doubles.
- [x] Preserve historical disjoint rebase: select the receipt revision chain, hydrate only local-patch entries missing a patch through `findCommitted`, verify commandId and before/after receipt identities, then apply the unchanged closure validation. Missing/inconsistent history fails closed. Current-session accepted records remain directly available.
- [x] Startup failure closes the worker and releases failed owner state. Worker death rejects all pending RPCs; accepted main-memory facts remain readable and save failure stays visible. Update the three existing writerFactory test doubles to the explicit initialization/history contract.
- [x] Tests: initialization with durable history and no historical patch/fact bodies in returned metadata; two edits+flush+read; duplicate save idempotence; historical rollback and disjoint rebase; pending/terminal Program outcomes across restart; initialization failure/worker death lifecycle; ordinary memory reads remain available while save is blocked. Use minimal synthetic fixtures and focused existing suites; no full-system/real-data run (controller owns integration).
- [x] Self-review and commit only assigned runtime/test files. Full report in SDD task-7-report.md with exact RED/GREEN and commands/results. Do not push, deploy, delete retained artifacts or modify business data. Clear `ATOM_RUNTIME_BACKUP_REPO` in all test commands.

**Remaining dependency after this task:** automatic compaction still needs reuse of same-repository verified metadata instead of rescanning historical patch bodies at the 128-record threshold; this stays open under Task 4 and must be measured/closed before claiming overall save efficiency.

**Implementation evidence, 2026-09-17:** `7321d4779276390add079ff9a953541561492519` implements Task 7. Final five worker/memory suites 30/30; unchanged transaction/publication suite 102/102 on the same runtime source (132 distinct tests, overlapping earlier runs not double-counted). Startup and historical rollback/flush reject main-process disk reads in the ownership regression; worker startup/exit failure cleanup and copy-transport resealing are covered. Independent `/root/runtime_review` verdict: spec compliant, quality Approved, Critical 0 / Important 0; focused checks verified metadata does not hydrate bodies, unchanged closure validation and publication/abort frames. Root source checks confirm the sole-owner boundary. Minor Git LF→CRLF environment notices are not Node test failures and require no runtime change. Task 7: complete (4497477..7321d47, review clean); real-scale acceptance remains pending. Harness fix round 1 proceeds on disjoint script/tests.

### Follow-on performance evidence to resolve after Task 7

- **Compaction:** `compactCommittedState()` calls `localRecords()` and `repairLocalTail()`, both reparsing the full log; frozen-baseline whole-world CAS also calls `localRecords()`. Reuse same-repository verified frame/cursor/command identity metadata with exact signature invalidation. Preserve the existing publication/abort proofs, Windows fallback and recovery behavior; no new log format is required by current evidence.
- **Steady interaction:** `readAtomContext(...committedSnapshot)` currently clones, normalizes, projects and freezes the entire committed world every time, producing a fresh array and bypassing `prepareTransformRelationIndex`'s WeakMap reuse. This is a source-confirmed candidate for the remaining measured multi-second reads. Resolve only after stage timing confirms the cost; reuse an owned deeply immutable version, never trust caller-supplied revision strings or shallow-frozen mutable facts as cache authority.
  - **Read-only copy probe, 2026-09-17:** On the retained DvN5OP baseline facts, three repetitions spent 250/162/164ms preparing committed context, 263/228/211ms rebuilding the relation index, and 80/77/80ms hashing; immediate reuse of the final immutable array/index took less than 1ms. These micro-measurements prove repeated same-version work, not end-to-end p50/p95. `legacy-engine-adapter` also clones its cached committed snapshot before every engine invocation, so fixing only the context-store WeakMap would miss the actual public path. A single owned immutable version must cross both boundaries. The probe accessed only retained copied facts and made no file writes.
- **Retry lifecycle:** verify worker failure does not permanently turn every retry into the same dead worker, while accepted memory remains authoritative. If recovery requires a new worker, re-open disk solely inside the saver and replay unsaved events idempotently; never reinitialize the active memory world from disk.

### Task 8: Bound same-owner compaction work (I3/U3/D2/E2)

**Scope:** `json-world-repository.mjs` and directly affected publication/transaction tests. Preserve Task 7 interfaces and unchanged on-disk formats. No production files or remote operations.

**Evidence:** after successful read/append, `compactCommittedState()` reparses the full historical log through both `localRecords()` and `repairLocalTail()`, although generation/watermark construction needs only committed identities and verified publication extent. `compareAndSwap()` repeats the full scan when baseline is frozen. These are recurrent work, not unavoidable cold recovery.

- [ ] Add a synthetic RED test with a counting filesystem: read once, append/compact under directory-sync supported and Windows-fallback branches; unchanged verified history must not be read/parsed again solely to compact. Test a sufficiently low compaction threshold, not a large fixture.
- [ ] Retain bounded command identity/publication extent metadata, never historical patch or fact bodies, on the same repository owner. Reuse it only when exact file/head/publication signature still matches. External append/replacement, failed/partial/aborted append, indeterminate durability and publication version change invalidate or update proof correctly; no caller-supplied trust token.
- [ ] Preserve all existing fsync, publication/abort, baseline/generation recovery and Windows fallback guarantees. In uncertain state perform the existing verified scan or fail closed. Do not truncate historical evidence merely to meet a timing target.
- [ ] Test external same-size replacement and incomplete-tail invalidation, failed append recovery, and restart reading the exact revision. Run directly affected publication/transaction tests before reporting; do not run full system/real-copy acceptance.
- [ ] Commit only scoped runtime/tests, retain copied artifacts and clear `ATOM_RUNTIME_BACKUP_REPO` for tests. Report RED/GREEN, I/O counts and any concerns in task-8-report.md. The controller owns real-scale timing and deployment.

**In-flight evidence, 2026-09-17:** Initial Task 8 counting tests 7/7 and existing transactions 102/102 passed: steady append/compaction full-log reads 2→0 per cycle in both directory-sync modes; proven Windows generation compaction 2→0 and frozen full write 1→0. Root review then identified a new cache-publication race: pairing an external post-write path signature with an older expected extent can bless unverified bytes. Implementer confirmed it; candidate is NOT accepted yet. Require fault-injected external append/replacement during proof publication, binding proof to the actual written file identity/extent and invalidating ordinary snapshot caching when proof is rejected. This extends existing exact-signature requirement, not the disk format or a multiprocess-lock protocol.

**Task 8 submitted:** `daa5d9afc27e2f9b55bf55234787d171162e02f1` closes the identified owned-write and stale-snapshot proof gaps. Final 12 compaction + 102 transaction tests = 114/114, 4285.7152ms; steady compaction/frozen full-write historical log reads remain 0, cold fallback removes the overlapping second full-history materialization (3→2 reads, second remaining read is the new small watermark). Independent `/root/runtime_review` assigned exact diff; not yet accepted. Controller read the full report and exact byte/count limits. Task 13's already-planned independent harness extension proceeds while this review is read-only; no concurrent implementation or promotion to real-scale testing.

**Task 8 review / fix round 1 queued:** Reviewer found Important stale-generation replacement in frozen-baseline `compareAndSwap`: external successor can arrive between successful `verifiedLocalMetadata()` and a later `compactionMetadata()` rescan; newer identities are then paired with old `nextSnapshot`, overwriting that successor. Root inspected the exact body and confirms the missing snapshot/metadata generation binding. Task 8 stays incomplete despite 114 green tests. Original implementer must add a focused between-checks CAS RED and bind/reject mismatched generation; no new disk format. Fix waits only for current disjoint Task 13 implementation to commit, preserving the one-implementation lane. No production switch or real-scale acceptance before fix/re-review.

**Task 8 fix round 1 RED:** At BASE12771f3 the injected external successor lands between frozen-CAS metadata checks, but CAS resolves instead of rejecting `LOCAL_WORLD_COMMIT_CHANGED` (0/1, 161.6911ms). The original implementer is binding the final compaction metadata signature to the generation whose snapshot was proved; uncertain fallback must reread before binding. No Task10 runtime changes are mixed into this fix.

**Task 8 fix round 1 submitted:** `05fb8df7cf065921e790d9163a8798cb15de0365` binds snapshot-proven and final metadata signatures. New regression GREEN1/1 and affected compaction/transaction115/115 (4038.8934ms); unchanged steady compaction and frozen-CAS paths retain zero historical-log reads. Root read exact code/report; original reviewer is checking fix-only12771f3..05fb8df. Task9 is independent of this storage fix and now starts on BASE05fb8df under the retained interaction agent; only one implementation lane is active.

**Task 8 closed:** Original reviewer confirms the sole Important finding addressed, no new Critical/Important, spec compliant and quality Approved. Root confirms the regression checks successful injection, rejected CAS, unchanged frozen baseline and cold readback of the successor. Task8 complete (8307ddd..daa5d9a plus scoped fix12771f3..05fb8df); no unreviewed persistence change remains in that task. Real-scale performance and production acceptance remain open.

### Task 9: Reuse a verified immutable interaction version (I3/U3/D2/E2)

**Scope:** `legacy-engine-adapter.mjs`, `context-store.mjs`, `world-revision.mjs`, plus the necessary existing consumers `engine.mjs`, `graph-server.mjs`, `cli.mjs`, `legacy-runtime-composition.mjs` and `legacy-projection-orchestrator.mjs`; focused tests only. No changes to command authorization, backup isolation, Program outcomes or disk formats.

**Evidence:** same-version public requests clone cached snapshots before the engine, then clone/normalize/project/freeze and rebuild identity-keyed relation indexes. Retained-copy micro-probe measured 450–590ms of avoidable preparation per repetition; immediate same-array reuse was under 1ms. This is not yet an end-to-end latency claim.

**Source risks for implementation:** `graph-server.currentAgentAuthorityOptions()` also calls `WorldService.readCommittedSnapshot()` per request, which clones again; its CLI `readAgentFacts()` hashes those facts and feeds context-store before authorization. Reuse must cover this consumer without changing authorization semantics. `world-revision.prepareWorldFactsRevision()` currently trusts `Object.isFrozen(facts)` as a cache key, while `freezeWorldFacts()` skips recursion if a container is already frozen; do not propagate that shallow-freeze assumption into the new trusted boundary. Add a regression before relying on that helper's seal.

**Ruling:** Adopt the read-only specialist's single owned committed-version descriptor: context-store constructs it once from isolated input, verifies revision/manifest, normalizes/validates and deep-freezes its context, and authenticates it via a private WeakMap brand. Adapter readiness holds the current descriptor by actual accepted/compatibility generation; internal `readCommittedVersion()` feeds engine, Agent and projection consumers, while external `readCommittedSnapshot()` keeps its isolated-copy contract. No caller flag or hash alone grants fast-path trust. This is a derived representation of the existing authority, not another fact owner. Reason: changing only context-store leaves the actual HTTP authorization/projection path cloning/rebuilding. Cost if wrong: consumer compatibility regression or excess per-version preparation; named caller/negative-input tests and independent review gate this change. Reuse `/root/interaction_performance` for implementation after the current lane, without re-reading session history.

- [x] RED: repeated Explore against one accepted version uses the same owned immutable context/index; a new accepted version invalidates reuse immediately, including A→B→A content cycles.
- [x] Establish an explicit owned/deeply-immutable boundary. Protect caller-owned mutable snapshots, shallow-frozen nested values, stale or forged revision strings, and changed compatibility manifests; such inputs cannot bless or poison a cache. Do not weaken public validation to hit a benchmark.
- [x] Reuse that version across the real legacy adapter → engine → context store boundary, not only in a direct helper test. Engine transforms must retain copy-on-write behavior and cannot mutate facts held by previous readers or in-flight saving.
- [x] Focused tests must cover current-version reuse, next-version read-your-writes, external mutable/forged input, compatibility-manifest invalidation, rejected writes, and concurrent old/new readers. Run directly affected context/interaction/Transform tests; report exact commands/results.
- [x] Commit scoped files/tests and report task-9-report.md. Do not run full system or real-data benchmark; controller will measure the same public journey after review.

**Task 9 in-flight RED/GREEN:** Shallow-frozen caller facts previously retained a stale revision after nested mutation; revision suite now5/5 GREEN. Two World Service reads previously created distinct Atom-array identities; the accepted-version identity regression is now1/1 GREEN after adapter/context wiring. Actual Graph-server two-request Agent identity remains RED (3 rebuilds/differing arrays), so Task9 is not complete and public consumer wiring continues. All tests have inherited backups disabled; no performance completion claimed.

**Task 9 public wiring GREEN:** The two-request real Graph-server Agent identity regression now shares one owned context across all3 observed security rebuilds. Core revision/context/WorldService/Graph/projection/memory-interaction group110/110 passes (18.6s); targeted Transform/interaction regressions, commit and independent review remain. This supersedes the preceding in-flight HTTP RED, not the measured real-scale latency.

**Task 9 submitted:** `3ba1aa4ba36c2f2507d700185efa10d05c700168`, core110/110 plus Transform/interaction138/138, eight runtime syntax checks and diff check pass. Independent runtime reviewer is checking1181e4e..3ba1aa4 (1181e4e contains only controller docs; runtime baseline matches dispatch05fb8df). Report discloses a discarded exploratory memory/real-engine fixture with WORLD_SAVE_ORDER_CONFLICT, replaced by a fake persistence port. Root requested its exact causal evidence instead of accepting a fixture-only label on the strength of unrelated GREENs. Task9 remains under review. Independent Task10 owns the next implementation lane at BASE3ba1aa4; no product/real-scale promotion depends on the unresolved fixture explanation.

**Task9 evidence correction:** Original implementer retracts the fixture-only inference. Minimal discarded sequence used real memory WorldService on Root/before, readCommittedVersion/readAtomContext, two Explore calls and one Transform to changed; the real worker rejected its durable beforeRevision with WORLD_SAVE_ORDER_CONFLICT. No per-await/expected-versus-actual evidence survived cleanup, so passing fake-port tests cannot classify this failure. Root will reconstruct against immutable3ba1aa4, retain fixtures and capture accepted batch versus durable revision. Reviewer otherwise has no confirmed code finding; Task9 approval stays pending the real failure's resolution. Do not downgrade this to noise or claim implementation complete merely from248 GREENs.

**Task9 controller reproduction:** Immutable3ba1aa4 real service + real worker succeeds for the reported sequence when explicitly closed with files present (`atom-version-order-repro-G8Rov2` retained); each read leaves physical revision5926204a unchanged, accepted record beforeRevision matches it, changed3776203a saves and closes successfully. A second tiny Node-hook diagnostic registers fixture retention by rename first and pending close second; hooks execute in registration order and reproduce the exact WORLD_SAVE_ORDER_CONFLICT in110ms (expected-error diagnostic1/1). No files were deleted; renamed fixture`atom-version-hook-repro-D4lVEz-preserved` remains. This identifies a concrete teardown-path cause, pending confirmation of the original hook order.

**Task9 fix round1 queued:** Committed new real-owned-version test adds recursive fs.rm cleanup contrary to this task's explicit artifact retention rule; root verified it in the scoped diff. Remove that new cleanup, retain resource closing, correct the report's unsupported classification, and restore the real-memory/real-engine integration regression with safe teardown. The existing fake-port test may still prove its narrower COW contract but is not a replacement for saving. Original interaction agent must wait until Task10 implementation finishes; no concurrent source edits.

**Task9 causal confirmation:** Original implementer located its discarded patch and confirms fs.rm was registered immediately after mkdtemp, before closeSaves was registered. This matches the controller's retained-by-rename fault injection and passing files-present control, so the reported ORDER_CONFLICT is now explained by teardown ordering, not an established runtime defect. Independent review records two Important fixes: remove new destructive cleanup and correct unsupported report attribution. Original agent accepts fix ownership and will restore the real durable integration regression after Task10 releases the lane.

**Task9 fixround1 closed:** `cb99bf8625f88e82c4cd99cfc3bd54d5048947b5` changes only tests/report. Real memory owner + actual engine retains old reader, accepts/reads new context, explicitly closes with files present, and cold durable recovery returns exact accepted revision/facts. Physical atom.json remaining baseline is expected journal-publication semantics, not a lost-save oracle. Minimal1/1 then affected48/48 passed. Original reviewer approves exact36a5474..cb99bf8:2Important addressed,0open/newCritical/Important. Task9 complete at E2; current measured write latency remains a separate open acceptance concern.

### Task 10: Restore independent saving after worker death (I3/U3/D2/E2)

**Scope:** durable-writer lifecycle and transactional saver composition, directly affected tests. If the writer contract gains explicit terminal state, forward that state through Task 13's measurement wrapper in `scripts/accept-real-world-write-copy.mjs`; do not redesign its measurements. Existing runtime memory remains sole authority throughout. No automatic production restart or external supervisor changes.

**Ruling from read-only lifecycle consult:** Use one owner-local serialized durable lane for saves and historical lookups, holding a replaceable writer. A terminal transport-death state is separate from ordinary RPC errors; close the old worker before creating/initializing its replacement, discard the replacement bootstrap payload and keep the live authority/ports intact. Corrupt-data initialization failure stays visible without recreating workers endlessly. Move unsaved-event release into the verified onSaved path: current owner clears events before the independent saver checks the returned revision, so a wrong watermark can otherwise destroy retry evidence. Task 10 supplies the no-replacement-after-close lifecycle gate; Task 11 adds bounded flush/shutdown. Reason: preserve single-writer/replay ordering without teaching the memory world to reload disk. Cost if wrong: duplicate writer or lost pending events; death/ACK-loss/wrong-watermark/close tests and independent review gate it.

- [x] Establish RED: accepted memory remains readable after writer death, and a later retry currently targets the same permanently failed worker. A recoverable process death must not leave every subsequent save doomed.
- [x] Replace a failed worker only inside the saver/serialized history lane, recover disk once in that replacement, and idempotently replay unsaved ordered events. Never seed or replace the active memory authority from the replacement's older disk snapshot. Ordinary transient save errors need not force a replacement.
- [x] Preserve one active durable writer, bounded configured retry scheduling, observable dirty/failure state, exact save watermark validation, pending RPC rejection and close semantics. No retry loop on shutdown after bounded failure.
- [x] Focused tests: death before save, after durable transaction but before acknowledgment, newer accepted writes during recovery, historical lookup, retry success and final restart; source/Program outcomes are not duplicated. Use tiny isolated fixtures and injected lifecycle boundaries, not production processes.
- [x] Commit scoped runtime/tests and report task-10-report.md; retain artifacts, disable inherited backups. Production acceptance remains Task 5.

**Task10 RED established:** Tiny lifecycle group13 cases has5 pass/8 fail (780.4336ms): dead retries retain the dead writer, lost acknowledgment stays stuck, wrong watermark drops retry events, failed close leaves5 attempts in20ms, and explicit terminal lifecycle state is absent. Implementation uses `writer.lifecycle {closed, terminalFailure}`, a serialized replaceable save/history lane, latched failed replacement initialization and release only in verified onSaved. The close-timer defect is handled at its owning scheduler boundary now; Task11 still owns timeout/public/process shutdown contracts. Task14/backpressure are not mixed into this patch.

### Task 11: Expose truthful save state through public replies (I3/U3/D2/E2)

**Task11 active checkpoint:** Sole implementer reused at BASE0272922; root documentation-only46e7663 may interleave. Shutdown core has validRED0/7 thenGREEN18/18 including existingTask10 lifecycle. Candidate bounds owner close, cancels waits, gates claim/prepare/stage/acceptance and strongly quarantines unconfirmed termination. Publicstate RED1/4pass,3expectedfailures: status lookup creates an owner, Program callbacks lack state, HTTP cachedreceipt replay lacks fresh state. Response-only decoration and actual graph entry deadline/signals are in progress; no independent approval or whole-task completion yet.

**Task11 publicstate checkpoint:** Publicstate/memoryinteraction/WorldService chain35/35GREEN. Ruling: after bounded close fails, the actual CLI signal entry reports a short structured failure and explicitly exits1; otherwise an unconfirmed worker termination could keep the process alive forever. This is abnormal/unsaved termination, never a successfulflush claim. Imported server.close remains non-exiting with strong owner quarantine. Repeated signals join oneclose/finalexit path; error-output completion itself must not introduce an unbounded wait. Cost if wrong: CLI process termination without a saved latest version, already visibly declared by the failure contract; accepted facts must not be silently called durable. Agent implements/testing actualsignal/shareddeadline next.

**Task11 graph checkpoint:** Actual signal/shareddeadline boundaryRED0/5→integratedgraph/public/ownerGREEN16/16. Candidate CLI emits a short synchronous structured `saved:false` failure beforeexit1; importedclose neverexits. Pending public stale-disjoint/overlap concurrency cases and full directly affected chain/selfreview remain before submission. No final task approval yet.

**Task11 additional isolation RED/GREEN:** Root's sourcecheck identified quarantinekey includesruntimeAuthority. Implementer proved a sameworlddisk facade can really commit while memorywriter termination is unconfirmed: focusedRED1fail, not merely objectcreation. Minimalcandidate fences the normalized worldkey independently of mode frombeginClose until confirmedtermination; normal nonquarantineddisk behavior stays unchanged. Currentnewtests19/19GREEN include actualengine stale disjoint edits surviving and overlapping staleedit rejectingWORLD_REVISION_CONFLICT. Final affectedchain and independentreview still pending.

**Task11 pre-submission evidence:** Broad affectedchain135/135passed before final selfreview. Later strictdeadline, oldfacade re-quarantine and cleanclose/hung-history findings each had targetedRED→fix. Final affectedowner/history/memory/public48/48passed2448.8894ms and final realHTTP acceptedstate+closebackup1/1passed1083.1157ms; preceding graphexpanded final-idempotence chain76/76passed. These are distinct revisions/scopes,not a claim that135 were rerun after the last cancellationfix. Report/commit and independentreview pending; rootdocs excluded from agent staging.

**Task11 submitted:** Runtime/tests commit7a235771b8587f48f992c282e0d0ca478154d648. Root read the full report, then dispatched retainedruntime reviewer on exact0272922..7a23577 package(includes interleaved documentation-only46e7663). No independentapproval yet. Rootwholeprocess crash andoldcode compatibilitydiagnostics have meanwhile passed against immutable0272922; finalcandidate reuse remains Task5,not evidence that changedshutdown code has been deployed.

**Task11 review risk pending proof:** Root and independentreviewer both identified that the new worldkey quarantine is checked during owner construction, while existingdiskfacade assertAccepting only examines memoryowner closing/closed flags and recover has no such check. Reviewer is running one isolated actualcommit/readback diagnosis for a diskfacade obtained before memoryclose. New-facade isolation tests do not settle this old-facade case. No fix verdict or completion inferred until the concrete diagnostic returns.

**Task11 Important confirmed:** Reviewer retainedfixture `C:/Users/worker/AppData/Local/Temp/atom-review-existing-disk-fence-TXQ1Hy`, validone-shotdiagnosticexit0/0.67s. Diskfacade created/read beforeclose really committed while sameworldmemorywriter close hadtimedout and raw.lifecycle.closed remainedfalse; a freshrepository read back the bypassrevision andsyntheticvalue. Sharedfence only at construction is insufficient. BlockTask11 on this finding and route one scopedfix to original implementer after the fulltaskreview returns, including writes/recovery admitted before/afterclose and asynchronous finalpublication rather than another constructor-onlycheck. Otherworlds and normal nonquarantined diskreads must remain valid. First diagnostic launch inherited `--input-type=module` into worker and was invalid; rerun used normalnode/dynamicimport, releaseditsclosegate and terminateditsownworker. No productioneffect.

**Task11 second Important:** Reviewer traced actual `atom-runtime-backup-trigger.mjs` running branch: flush only sets pending and immediatelyreturns while an older backup isrunning; graphclose then closes thetrigger,preventing that pending latestsave backup. Thus successfulclose does not yet prove it awaited the finalbackup. This is a pre-existing helper semantic gap exposed by Task11's required closeintegration,not a claimed newlyintroducedalgorithmregression. Include its minimal drain/await repair and held-realtrigger regression in the sameTask11fixround; shareddeadline and no realbackupdestination test constraints remain.

**Task11 fix round1/5 active:** Original implementer `save_architecture` owns the sole implementation lane at FIX_BASE7a235771b8587f48f992c282e0d0ca478154d648. Both Important findings are carried together; original reviewer will receive only the fix range. Ruling: enforce the shared world quarantine through the existing disk repository/journal fileSystem capability, including writes on already-open handles and mutating recovery after awaits, in addition to facade admission. Constructor-only checks cannot establish final publication isolation. Preserve ordinary reads and other worlds. The actual backup trigger must expose an awaitable drain of the running backup and necessary pending latest backup; timer/start failure observation and graph's single absolute deadline remain intact. Cost if wrong: either an old writer can publish into a quarantined world or shutdown reports success without the final backup; held-I/O and held-backup regressions gate both boundaries. No new ownership framework or backup destination is introduced.

**Task11 fix round1 targeted checkpoint:** Implementer reports26/26 GREEN on old disk commit rejection, already-open world/journal handles across awaits, nonmutating recovery/read availability, mutating recovery rejection, independent world writes, and actual backup-trigger held-old→latest drain plus visible false-return failure. This is not final-source approval. Root identified an in-flight capability tail to verify: already-dispatched OS calls are not cancelled by dispatch fencing; once memory termination removes quarantine, an older suspended disk operation must not revive into the replacement owner. Implementer is testing that exact race before the affected suite and scoped review. No claimed guarantee that fencing retracts an OS call already issued.

**Task11 tail race RED/GREEN:** A held completion of an already-dispatched disk open reproduced early successful memory close and quarantine removal (RED1/1, retained `atom-world-shutdown-0PK9Pc`). Minimal candidate27/27GREEN permanently revokes existing disk-owner write capability at beginClose and retires it from the weak registry; termination confirmation also awaits already-dispatched mutation promises. Ruling: retain quarantine until both writer termination and that finite I/O tail settle, bounded externally by the same shutdown deadline. Old capability never reactivates; a new facade receives a new capability only after safe release. Already-issued OS effects may finish, but no subsequent publication or replacement owner crosses an unconfirmed tail. Cost if wrong: old/new writer overlap after reopening; the held-open/late-publication regression and scoped review gate this. Final affected tests/report remain pending.

**Task11 fix round1 submitted:**0dfb75a0da863aff8d59894028f20988e95c6644, exact FIX_BASE7a23577 range includes root docs e2d996e. Final eight affected files189/189pass,7318.1509ms; root read appended report and capability/drain implementation, then resumed original reviewer with fix-only package. Intermediate recovery test188/189 was an incorrect oracle attempting to bypass existing LOCAL_WORLD_COMMIT_RECOVERY_PENDING; corrected test requires old baseline plus safe-release fresh-capability recovery, with no runtime guard weakened. Concurrent backup flush successor had an actual intermediate failure and was fixed to await active successor as well as pending. No independent fix approval yet; no full suite/production/remote action.

**Task11 scoped-review doubt under validation:** The new fenced filesystem also blocks persistSnapshot's failure cleanup. Whole-world fallback creates its final revision-named snapshot with wx before awaiting gzip/write; revocation at that await may leave an empty canonical object, and a fresh owner may then fail EEXIST→readSnapshot. Root verified that code path; reviewer is running one synthetic retained-fixture reproduction to distinguish a permanent retry obstruction from normal fail-closed recovery. This is not yet a confirmed third finding or permission to bypass the fence; Task17 remains behind the Task11 gate.

**Task11 fix round1/5 verdict:** Original2Important ADDRESSED, new1Important,0Critical/Minor. Valid reviewer diagnosisexit0/.443s retained `C:/Users/worker/AppData/Local/Temp/atom-review-revoked-snapshot-4WCVym`: held snapshot wx completion→close timeout→oldcommit quarantined; after safe release fresh recover reports0 but fresh whole-world commit fails TRANSACTION_SNAPSHOT_READ_FAILED against a0byte canonical snapshot. Baseline unchanged; this is persistent EEXIST poisoning, not pending recovery. Initial unsealed-seed diagnostic was invalid and excluded. No outside-scope observations.

**Task11 fix round2/5 active:** Original implementer resumed at FIX_BASE0dfb75a with that exact finding. Ruling: preserve canonical revision names/schema/hash and the revoked-capability boundary; publish an immutable snapshot only after complete private temporary-object write/sync, using an atomic non-overwriting publication primitive that is itself fenced/tracked. Never allow arbitrary stale cleanup to bypass fencing or silently replace a corrupted existing canonical object. Unpublished temporary residue is preferable to poisoning the authoritative object name. Cost if wrong: stranded temp objects or broken same-revision concurrency; tests must cover held creation→close→safe new-capability commit/cold read, preexisting-object verification and same-revision concurrent creation. Original deadline, backup drain, Task8 publication proof and retained-fixture constraints remain; no broad storage-format redesign.

**Task11 fix round2 RED:** Focused test0/1failed335.5454ms; fresh disk recover0 followed by same-baseline commit throws TRANSACTION_SNAPSHOT_READ_FAILED/Z_BUF_ERROR after old wx creation is revoked. Fixture `atom-world-shutdown-WBjtru` retained. Implementer selected same-directory unique temporary gzip file, complete write/sync/close, then fs.link for atomic non-overwriting publication; link joins the existing fence/dispatch tracking. Canonical name/SHA/schema and cleanup revocation remain unchanged. GREEN and review pending.

**Task11 fix round2 targeted GREEN:** Snapshot revocation/retry, concurrent same-revision publication, existing corrupt-object verification, write/sync failure5/5pass504.1306ms; link-dispatch revocation and already-dispatched tail isolation2/2pass800.5919ms. Root inspected the actual diff: canonical object is first verified if present; otherwise complete temporary content is linked without overwriting a winner, which is independently verified on EEXIST. Cleanup remains fenced and only targets the operation's own temporary path. Final affected chain/commit/scoped review pending; no blanket completion claim.

**Task11 fix round2 submitted:**51381598bbb5038a7eadf72fbfcc955a42c78a05,4scopedsource/testfiles. Final transaction/compaction/worldshutdown/realbackuptrigger/graphshutdown152/152pass10525.9934ms; original reviewer now checks exact0dfb75a..5138159 package. Root inspected diff/report. Concurrent same-revision publication also had validRED1fail/4tests before atomic linking. Actual Windows retained fixtures exercised hard links successfully; unsupported filesystems fail closed without unsafe rename fallback. No additional directory-sync/power-loss guarantee is claimed. Root plan/ledger are outside agent staging; production/remote/fullsuite unchanged.

**Task11 closed at E2:** Independent fix-only0dfb75a..5138159 re-review: sole remaining Important ADDRESSED,0newCritical/Important/Minor, Spec compliant and Quality Approved. Root agrees with actual source/publication tests; original2findings were already addressed inround1. Task11 complete through5138159, not a claim of finalCLI presentation(Task18), performance, fullsuite or deployment. Retained interaction agent now receives Task17 at BASE5138159; original architecture and runtime-review sessions remain reusable.

**Prerequisite checkpoint:** Task10 submitted `36a5474ba4fb80ff6a52d007cdad8eccbbe5217a`; lifecycle/saver/worker/memory affected suites52/52 pass,2384.5523ms. Root read the report and actual owner/saver source; exact3ba1aa4..36a5474 independent review is active. Verified release now occurs only after returned revision and authority watermark checks. No claim of bounded close, body demotion or production performance follows from this. Task9 test/report fixround1 owns the sole implementation lane at BASE36a5474 while review is read-only.

**Task10 review closed:** Exact3ba1aa4..36a5474 independently Approved,0Critical/Important. Root source check agrees: authority watermark validation precedes replay-evidence removal, replacement bootstrap never reseeds current facts, and history shares the durable lane. Task10 complete at E2; Task11/14/15 remain separate obligations. Root now profiles the unchanged36a5474 revision in detached `../memory-profile-36a5474`, real-copy two-sample journey plus CPU profiles,420-second owned-process watchdog, retained data/logs and inherited backups disabled. Profiling overhead means this is diagnostic timing, not final latency acceptance; source worktree and production remain untouched.

**Scope:** existing World Service/legacy adapter/public response composition and directly affected CLI/HTTP/Program tests. This implements Task 5's existing public-state requirement, not a new control API. Current source exposes `saveStatus()` only internally.

**Controller technical ruling from retained architecture preflight:** Default configurable shutdown timeout30,000ms, one absolute deadline through graph-server→service→owner/drain/backup rather than each layer resetting its allowance. Stop listening and close write admission before potentially hanging interaction drain; flush already accepted evidence concurrently with bounded draining. Owner cancellation rejects queued/initializing/RPC waits and suppresses lateACK onSaved; at timeout directly terminate only its writer without first waiting for the hung durable lane. Release owner registry only after confirmed writer termination. An unconfirmed termination remains strongly quarantined (current weakref registry alone is insufficient), rejects reopening, and still returns bounded failure. Successful save precedes backup flush/close; timeout must not be called saved. Supported orderlySIGINT/SIGTERM reuse one close; importedclose does not exitprocess and Windowsforcedstop remainscrash.

`ports.beginClose()` must synchronously gate prepare/stage/finalcommit/outcome before mutations and clear unaccepted prepared/staged records so later read-recovery cannot publish them; acceptedreceipt reads/idempotent rereads remain valid. Status capture reads only an existingowner and never triggers recovery/history/flush. **HTTP replay boundary:** cli/lib/server.mjs caches reply promises in atomCommandReceipts and bypasses legacyadapter on repeat IDs, so each actual HTTP emission also captures fresh saveState. Decorate copies, not cachedbusinessresults or outcome.result. This is required by the existing publicstate contract; no new remote endpoint or permission policy.

- [x] RED: memory-mode public Transform and subsequent Explore replies distinguish accepted and saved revisions while saving is held, then show the caught-up watermark after flush; failure stays readable and is visible in the reply.
- [x] Exercise two fresh public writes while saving is held: disjoint edits both survive; overlapping stale edits obey the existing conflict rule without silent overwrite. Subsequent public reads must see accepted facts before releasing the save gate. This closes Task 4/5's existing multi-edit/public concurrency requirement, not a new API.
- [x] Attach a content-free `saveState` object to memory-mode results using the existing status contract (`acceptedVersion`, `acceptedRevision`, `savedVersion`, `savedRevision`, `pending`, `failure`, relevant auxiliary failure). Define it as world status at response capture, not proof that a historical/replayed command just changed facts. Do not overwrite command-specific revision fields or claim a pending Program outcome is saved just because world revisions match. Preserve disk-mode response compatibility.
- [x] Cover intermediate source-accepted notifications, final/replayed Program outcomes, normal replies and subsequent reads; retained outcome results must not freeze a stale save state forever. Status collection must not await flush or reload durable history.
- [x] Bound normal close/flush waiting at the shutdown boundary with an explicit configured timeout and visible failure; terminate/release only the world writer being closed, never another world's active service. Close must disable further retry timers even when flush fails or times out. Existing successful close still verifies saving and backup ordering. Test a hung saver and saved older/newer memory versions.
- [x] Quiesce write admission before closing the saver: an old facade or an in-flight prepared operation must not publish new memory facts then fail in the post-accept enqueue hook after close begins. Define/drain already accepted work, reject nonaccepted late operations before mutation, and test completion racing close/timeout plus reopening via a new owner. Reads of accepted facts remain honest; do not label a forced timeout as saved.
- [x] Bind supported orderly process termination signals in the actual `graph-server.mjs` entry to that close path; currently only an imported server's `close()` calls it. Forced OS termination or `Stop-ScheduledTask` remains crash semantics and must not be called a verified flush. Do not introduce an unauthenticated remote shutdown API as a shortcut.
- [x] Run focused public tests, commit scoped files and report task-11-report.md. No deployment or full suite. Controller owns public copy benchmark and final acceptance.

### Task 12: Verify Web translation consumes current memory facts (I3/U3/D2/E2)

**Scope:** `legacy-runtime-composition.mjs` human workspace/status translators and focused public Web tests. Current source reads `graphFile` for target/relationship translation although projection publication is delayed; derived disk Graph must not become a competing interaction fact source.

**Read-only preflight at7a23577:** Retained acceptance agent located the existing synthetic HTTP fixture in `tests/atom-language-graph-server.test.mjs:399`. Exercise actual `/__atom/api/workspace-edit` and `/__atom/api/human-status`, then public `/__atom/api/command` Explore; no browser is required for this translation boundary. Use memory mode with held/long saving and Graph publication boundaries, verifying Graph/disk remain old while memory changes. Existing `worldService.readCommittedVersion({contextFile,projectionFile:graphFile})` supplies facts/revision/compatibilityManifest, with readCommittedSnapshot fallback; translators presently lack that provider. Existing translator tests around1372 prove only disk Graph/explicit paths. Suspected gaps remain untested: new node absent in stale Graph, renamed key resolving old path, relation replacement built from stale strut, and deleted-target status fallback. Establish actual REDs before changing semantics; this preflight ran no tests and made no implementation claim.

- [ ] Establish the actual public regression before changing code: while Graph publication and saving lag, accept a node creation/rename or relation change, then perform the next Web edit using its current semantic target. Verify the translator can resolve current memory facts and does not rewrite current relations from stale Graph. If the real journey disproves the suspected defect, record evidence and make no speculative change.
- [ ] When reproduced, inject the existing committed-snapshot provider into the translator composition, deriving its lookup from the same owned current version. Preserve stable identity/path validation, human authority boundaries, exact ambiguity rejection and standalone translator compatibility. Do not await disk saving or force synchronous Graph publication merely to make the next edit work.
- [ ] Test creation→edit, rename→edit, relation preservation, removed/stale target failure and disk-mode compatibility through existing public surfaces. Use isolated fixtures and held projection/save boundaries; no production data.
- [ ] Commit only scoped files/tests and report task-12-report.md with RED/GREEN and exact outputs; no full suite/deployment. Any broader UI behavior remains outside this task.

### Task 13: Measure repeated public interaction and independent saves (I3/U3/D1/E2)

**Scope:** `scripts/accept-real-world-write-copy.mjs` and its focused tests. Reuse reviewed Task 6 baseline/rollback boundaries. This operationalizes Task 5's existing performance acceptance without adding a production API.

- [x] Add opt-in bounded `--latency-samples N` (default no repetition; allowed 2–20) to repeatedly replace the copied acceptance node's situation and immediately Explore the exact newly written value. Every sample gets a fresh interaction ID; never benchmark a cached idempotent response as a new write.
- [x] Output numeric sample counts and read/write p50/p95 plus event-loop delay; do not output business facts. Monitor delay through reads and final save completion, distinguishing interaction-window and save-window measurements where possible.
- [x] Inject an explicitly memory-mode World Service into the copied server when measuring memory mode. Its writerFactory can wrap the real writer to measure `save()` RPC duration; `onAuthoritativeWrite`/`onSaved` and accepted/saved status can measure version-correct save lag. Report RPC duration and flush/close wait under distinct names; never call total close wait pure save I/O. Preserve initialize/findCommitted/close contracts. Do not disable or artificially slow the real save schedule for the performance run.
- [x] Restart explicitly in the same requested mode and verify public readback/current save status. Keep an independent disk-mode restart compatibility assertion rather than mislabeling disk restart as memory-mode restart.
- [x] Synthetic tests cover argument bounds, fresh sample identities/readback, percentile calculation, measured saves, monitoring through flush, same-mode restart, old/pending-history preservation and source-byte invariance. Keep copies and backups disabled. Commit scoped files, report task-13-report.md; root owns expensive real-copy run with an outer process timeout.

**Acceptance metric limitation:** Task 8 counting-filesystem tests establish full-log read elimination and byte/count behavior on exact changed persistence paths. The public benchmark reports saver RPC wall time and version-correct lag, not invented physical-disk bytes from net file growth. Any claimed actual I/O volume requires direct instrumentation; do not label file-size deltas as bytes written.

**Direct byte instrumentation preflight:** Root verified local Node `--trace-event-categories=node.fs.async` records completed native read/write byte counts and fsync, including durable-style Worker threads. Retained synthetic evidence `C:/Users/worker/AppData/Local/Temp/atom-fs-trace-check-970c5c775e7c4e2b9b34932c2a5dac3c`: main/worker reads each61bytes; exclusive-created `synthetic-byte-probe.txt` write21bytes and fsync result0. Trace timestamps match process.hrtime microseconds (read ts12439214611 lies between12439210219 and12439214844), enabling precise stage delimiters from a diagnostic stderr wrapper without product-code changes. The attempted usertiming category did not emit marks and is not used. For the next immutable-source diagnostic, delimit interaction/save using monotonic stage marks and aggregate only the identified durable-writer thread's native file transfers, separate from startup/recovery. These are actual file-API transfer bytes, not physical storage-device bytes or net file growth. No production facts were touched and all diagnostic artifacts remain retained.

**Task 13 submitted, 2026-09-17:** `12771f3af7b6acac6505e183fe8fbfbe0be8f76e` adds bounded repeated public samples and separate save/watermark metrics; synthetic acceptance 8/8, named source-change guard 1/1, both syntax and scoped diff checks pass. Original sample/bounds REDs are recorded in `task-13-report.md`. Root read the report and measurement source; independent harness reviewer is checking exact daa5d9a..12771f3 diff. This is not real-scale acceptance. A zero save-window read-probe count is inconclusive, not proof of concurrent reads. Task 8 fix round 1 now owns the sole implementation lane at BASE12771f3; no production or remote changes.

**Task 13 review closed:** `/root/harness_review` independently approves exact daa5d9a..12771f3, Critical 0 / Important 0. Root confirms the final pending-false status joins saved version and revision, and that save RPC wall time is not mislabeled disk I/O. Task 13 complete at E2; real-copy metrics, observed concurrent-save reads and outer watchdog remain root acceptance duties.

**Next real-copy storage check:** With Tasks6/7/8/13 reviewed, run the public copied-world journey on an immutable detached `05fb8df` worktree while Task9 edits the separate development worktree. This is the first corrected real-scale storage/recovery check after the baseline-oracle repair, not final interaction-performance acceptance. Use two fresh samples, inherited backups disabled, preserved copies/logs and a 420-second outer timeout that targets only the process it creates. Isolating source avoids dynamically imported worker code changing during measurement; no production writes or switch.

**Real-copy storage journey GREEN, interaction performance still open (05fb8df):** Outer process exited0 without timeout/OOM. Baseline30.414s, startup36.674s, interaction22.515s, final save/close21ms, rollback53.841s, memory restart34.392s. All writes immediately returned their exact accepted values on subsequent public reads; only3 new commits rolled back, restored source revision, same-memory restart watermarks/public readback and source-byte guard all true. Copy retained at `C:/Users/worker/AppData/Local/Temp/atom-real-write-acceptance-u0QegH`; raw output/stage logs at `C:/Users/worker/AppData/Local/Temp/atom-memory-save-probe-e66d2b5481d246bb84fa273895bf8349/`. No production files changed.

Two fresh samples: write p50/p95=4638.793/5838.078ms; read=2468.404/3002.050ms. Save RPC wall times112.490/2278.235/3453.942ms, final accepted-to-durable lag5592.167ms; flushWait0.075ms and closeWait20.836ms are separate waits. Main-loop maximum delay3583ms remains unacceptable. Save-window probes0, so this run does NOT establish concurrent-read responsiveness during saving (tiny held-save regressions remain separate evidence). Context preparation275–331ms repeats; Program projection rebase spans2562/7537/2471ms may include concurrent event-loop delay and are not isolated CPU measurements. This closes the corrected real-scale storage completion/OOM concern for this journey only; Task9 plus subsequent measured critical-path work remain necessary before final performance/E3 acceptance.

### Remaining long-lived-memory contract audit

**Fixed-source CPU diagnostic36a5474 completed:** Detached code and copied world `C:/Users/worker/AppData/Local/Temp/atom-real-write-acceptance-kS1Ajh`; logs and V8 profiles retained at `C:/Users/worker/AppData/Local/Temp/atom-memory-profile-c51c8d053d4b43be93744ed2496595ac`. Exit0, no timeout/OOM, exact immediate readback, only3 newcommits rolled back, source revision restored, same-memory restart/readback/watermark and source-byte invariance all true. Initialwrite3157ms/read99ms; two fresh write samples2619.458/2716.302ms, read1673.418/760.221ms. Thus idle same-version reads improve but overlapping write/postcommit CPU still delays subsequent reads; no claim of uniform100ms latency. Interaction maximum delay1611ms. Two save-window reads succeed, save-window maximumdelay17ms; this is actual observed independent-save/read evidence. SaverRPC wall335.111/916.109/1669.499/1717.002ms, finalaccept→durablelag2202.608ms; flushWait0.192ms,closeWait35.050ms are separate waits. Diagnostic CPU profiling adds overhead, so final acceptance must use the unprofiled final candidate.

Mainprofile `CPU.20260917.115925.31148.0.001.cpuprofile`, approximate interaction slice68.0–79.6s (stage-aligned, not an exact per-request CPU timer), attributes~3.39s inclusive to repeated revision stringify/hash,~2.16s to key parsing,~1.02s to clone,~0.75s GC. Hash stacks span engine candidate, coordinator validation, memory commit sealing, cloned committed-version hash+manifest validation+seal, and Program refresh/security over unsealed candidate arrays. Each new owned-version preparation costs465–525ms. Program projection spans include overlapping interactions and must not be treated as pure rebase CPU. Root independently read these callers; retained interaction agent is doing a bounded read-only safety consultation before the next scoped latency fix. Task14 already owns the sole implementation lane at BASEcb99bf8; no source code was changed for profiling.

Root source inspection and retained architecture agent independently confirm two different retention boundaries remain open. `memory-transaction-ports.accepted` never releases full accepted record bodies after saving, so healthy sessions accumulate historical patches/full snapshots; metadata-only startup alone does not solve this. Separately, owner `unsaved`/`savedWorldVersions` and authority unsaved revisions have no finite outage backlog bound. Saver coalesces a watermark, not ordered evidence. These are existing specification recovery/backpressure obligations, not optional new features; neither is claimed fixed by current tests.

The verified onSaved boundary from Task10 is prerequisite to safe body demotion. Pending admission is a separate policy: rejection must occur before prepare/accept or outcome mutation, never in the current post-accept notification hook. Already accepted evidence must be retained and reads must remain ungated. Program continuation cannot bypass all limits; bounded reservation or deferred launch and explicit retry semantics require a concrete design before implementation. Do not quietly choose arbitrary business-size limits while fixing Task10; settle the technical configuration and semantic contract separately under the existing specification.

**Old-code compatibility preflight:** Retained harness agent inspected exact rollback86a5e8c APIs; root confirmed the persistence rollback entry. After final-candidate flush, create a fresh tiny synthetic compatibility copy containing ordered local commits and completed/failed Program outcomes, with a plain non-Program edit last. Run that exact old checkout's persistence reader on the copy with backups disabled: assert current revision/facts and outcome bindings/statuses, then rollback only the final test edit and assert restoredCommandId, prior revision and exact readback. This proves actual inverse-patch compatibility, not merely matching format fields. Do not use the old acceptance script or feed the multi-gigabyte private journal to an old full-history reader. Execution remains pending under Task5; no compatibility success is claimed yet.

### Task 14: Release durable history bodies from the live owner (I3/U3/D2/E2)

**Dependency/scope:** Task10 verified watermark first; memory transaction ports and its onSaved composition plus focused tests. No disk format or authority reset.

**In-flight Task14 evidence:** Behavioural RED6cases/1pass/5fail proves saved entries still retain bodies and saved rollback/rebase skip durable hydration. A first incomplete rebase fixture lacked explicit closure arrays; corrected before this valid RED. Minimal candidate6/6GREEN moves only authority-verified acknowledged versions into ordered durable metadata and removes accepted body references; full affected chain/review still pending. No capacity/format changes.

**Task14 closed:** `031da6f39ace8bd6e924b69660e1c00fb25e9d71`, final directly affected161/161pass,9216.6501ms. Independent exactcb99bf8..031da6f review Approved0Critical/Important; root read report and markSaved body, confirms watermark validation before body/event release, immutable old readers, saved-history hydration and version-based ordering. Scope proves release of live-owner body references, not a measured RSS reduction or bounded historical metadata. Task16 now owns sole implementation at documentation-onlyc2d9218 base; original architecture agent does only Task11 preflight concurrently.

- [x] RED: after a successful exact save, live receipt/history enumeration must no longer retain full record bodies; unsaved bodies remain present and fully reversible. Wrong watermarks/failures must not demote anything.
- [x] Demote only the verified saved prefix to ordered `{commandId, historyMode, receipt}` metadata. Preserve latest receipt, Program/idempotency lookup, A→B→A version ordering and current facts. Historical `findCommitted` must detect metadata-only entries and retrieve complete evidence through the existing serialized worker lane, not shadow it with a metadata object.
- [x] Exercise saved historical rollback/disjoint rebase, failed-save retry, pending/terminal Program outcomes and concurrent new acceptance; bounded in-flight readers retain their old immutable objects safely while the owner releases references.
- [x] Run only affected memory/worker/transaction tests; commit scoped files and `task-14-report.md`. Root owns repeat public performance and final acceptance; no production/remote/deletions.

### Task 15: Bound pending admission without losing accepted work (I3/U3/D3/E2)

**Dependencies/scope:** Task10 verified save/lifecycle contract and Task14 retained-versus-durable history boundary. Existing memory transaction ports, saver composition, Program continuation adapter and focused tests only. Follow the technical capacity ruling in the existing design specification; no new disk format, remote control endpoint or generic queue framework.

- [ ] RED: permanently failing/held saves currently permit unbounded new accepted events. Exercise finite count/byte limits before publication, rejected writes leaving fact/version/receipt unchanged, and reads remaining available. Count-based coalescing alone is not a payload-byte bound.
- [ ] Implement configurable logical pending limits (defaults512MiB/2048events/64MiB single event), exact pre-accept reservation/release and verified-save release. Local edits account only local evidence; full records reuse prepared serialization lengths. Report accounting separately from physical RSS. No accepted evidence may be evicted to satisfy a cap.
- [ ] Reserve bounded first-pending/final-outcome capacity at new source admission; insufficient capacity rejects before source acceptance. Continuation capacity failures remain retryable pending, not terminal business failure. Historical pending obtains capacity incrementally; verified release re-arms one coalesced resume rather than repeated attempts while full. Preserve child receipt idempotency and oversized-outcome visibility without silently truncating business results.
- [ ] Test failure/recovery, abort/rejected command release, A→B→A, old save versus new accepts, parallel sources, child already committed, restored pending history, oversized event/outcome and close. Expose content-free pressure/accounting in existing save status; no permission or backup boundary changes.
- [ ] Commit scoped changes and task-15-report.md after focused RED/GREEN; independent review must confirm atomic admission and Program semantics. Root owns physical-memory/performance measurements and E3 rollout.

### Further latency ruling under preparation

Root content-free count of the retained36a5474 copied baseline:12,283totalAtoms,43activeAtoms,12,240archivedAtoms,1typeddefaultbackup root. CompactJSON14,688,181bytes, archiveJSON14,623,309bytes. More than99% of bytes are inactive. Actual Program records stop below the backup root, but their prerequisite global boundary scan and revision hashing still traverse the whole world; multiple engine/Transform full clones also destroy unchanged archive identity. Do not claim archive has zero interaction cost. Next optimization must retain globally validated identity/default-root/restore semantics while avoiding repeat work over unchanged immutable archive subtrees and handing off an owned immutable candidate safely. Existing flatSHA256 contentrevision still needs one changed-world computation unless the proof boundary changes; duplicatehash reduction is not proof of zero global hashing. Retained interaction agent is returning exact scope/safety obligations before implementation; no new format or speculative worker framework is authorized by this note.

### Task 16: Carry one owned validated candidate through acceptance and consumers (I3/U3/D3/E2)

**Evidence/ordering:** The36a5474 main-loop profile attributes repeated full-world hashing/cloning across engine candidate→persistence→coordinator→memoryports→cloned public snapshot→owned-context preparation→Program consumers. This is the next measured critical-path task after Task14, before remaining public-state/translation/capacity work; urgency follows the user's still-failing ordinary read/write goal, not task ease. Task14 changes must be preserved. This is the first bounded optimization, not a claim to finish all archive/COW or eliminate the canonical hash.

**Scope:** final candidate boundary in engine, world-revision ownership proof, memory ports/transactional persistence internal read boundary, context-store/legacy-adapter handoff and directly affected tests. Public snapshot copies, revision bytes, disk formats, conflict/rollback, authorization and Program semantics remain unchanged. No new generic cache, caller-controlled trust flag, worker framework or deployment.

**Initial Task16 RED:** Revision tests5pass/2fail because exportedseal accepted a frozen getter and nested getter/Map. Existing plain-data hash/reuse and unsealedgetter recomputation pass; the former stablegetter counter-based tests are being corrected to plain-data/realwork oracles, not used as permission to cache mutable accessors. This defect must close before widening immutablecandidate reuse. Identity/handoff RED follows separately; no latency completion claimed.

**Task16 owned-path RED/GREEN in flight:** Real memory/engine regression initially fails because accepted readCommittedVersion.facts differs from committed engine candidate identity (retained eenkIpfixture). First candidate now passes that identity regression1/1,232.6ms, including detached publiccopy, oldcontext and cold recovery; focused owned/untrustedcontext1/1 and claimed/unclaimedmemoryport1/1, revision safety8/8. These are preliminary local results; affected Program/rollback/consumer chain and independent review remain. Root spotted repeatedalias-vs-cycle and sparsearray proof subtleties and the localized access inheritance hook running before ordinary candidates were frozen; implementer is resolving these within Task16's ownership boundary, not weakening public input checks.

**Task16 affected chain GREEN, review pending:** Six revision/context/memory/service files70/70 and five Program/Transform/HTTP files131/131 pass with inherited backups disabled. Real Program assertion observes the exact accepted candidate; initial missing-projection fixture was corrected with its required reconcile runtime mode before GREEN. Root read the ownership/seal changes, including rejection of proxy/accessor/exotic values, alias-versus-cycle handling and dense-array proof. The existing access-inheritance hook stays unchanged in this task: moving sealing ahead of it would activate another full-world `walkAtoms`, not establish archive isolation. That measured boundary remains separate; no final latency claim. Implementer is preparing scoped commit/report.

**Task16 submitted:** Exactc2d9218..0272922f8b320ff89bde901b70b2934e130984e2;201 affected tests plus final7memoryport tests pass. Root read the report and actual changes; retained runtime reviewer is checking the exact packaged diff independently. Detached diagnostic source `../memory-trace-0272922` isolates the next copied-world measurement from later development. Strictly, downstream sealed identity avoids repeated hash work; earlier changed/revision computation can still hash before sealing, so the entire interaction is not claimed to hash only once. No production or remote change.

**Task16 review closed:** Independent exactc2d9218..0272922 review Approved,0Critical/Important/Minor. Reviewer confirmed original precommit and coordinator checks, frozen candidate consumers and detached public copies. Root agrees on ownership correctness; measured latency remains open rather than being inferred from identity tests. Real-copy diagnostic is running against immutable0272922 with native file-API trace, monotonic stage marks and no CPU profiler; preserved logs `C:/Users/worker/AppData/Local/Temp/atom-memory-trace-a5905d22e0d0483381f7e924987f564e`, ownedPID34708,420second outer watchdog. Task11 may now use the sole development lane while root completes that independent measurement.

**0272922 copied-world diagnostic complete:** Exit0 in246.361s, no timeout/OOM. Copy `C:/Users/worker/AppData/Local/Temp/atom-real-write-acceptance-qO8AGL`; raw stdout/stderr and19MB native trace retained in the above directory. Exact immediate readback,3new-only rollbacks, restored source revision, same-memory restart/public readback/watermarks and unchanged source bytes all true. Baseline45.007s,startup49.003s,interaction9.976s,flush595ms,rollback86.012s,restart47.684s,finalclose197ms. Initialwrite2504ms/read154ms; fresh writes2402.624/2796.314ms(p50/p95), reads997.462/1107.789ms. Mainloopmax1329ms: ordinary interaction remains unacceptable. Ownedcontext preparation257–307ms still includes full validation. Saving window3reads succeeded,maxdelay95ms; saverRPC145.800/692.557/2072.874ms, finalaccept→durablelag2827.728ms,flushWait0.249ms,closeWait34.626ms. No CPU profiler was enabled, but native tracing and different runtime load mean this is not a controlled percent-improvement claim.

**Direct real-copy I/O evidence:** Native `node.fs.async` completed read/write results, delimited by verified process.hrtime stage marks. Initial writer identified asTID13932 by its startup journal/world opens; mainTID11016, restartedwriterTID29616. During interaction-start13296791960us throughflush-end13307364547us, writer transferred786,432readbytes(12reads,each65,536) and2,117,769writebytes(21writes),21successfulfsync plus3directoryfsync failures(-4048). Root matched the failed directory-sync position to the existing Windows fallback; recovery/watermark verification still passed and no directory-durability success is claimed. Worker startup separately read1,862,535,357bytes; this cold cost is not repeated in each save. Main thread in the interaction/save window read1,101bytes and wrote63,604bytes of projection/diagnostic work. These counts are file-API transfers, not physical-device bytes or net file growth. Tail inspection, without logging fact content, shows each new world record is local-patch sized(~1KB) while transaction receipt/record/outcome envelopes contribute most remaining bytes; no full-world rewrite occurred in this observed window. Remaining frontend CPU and archive/index reuse stay open. Probe process34708 and exec89809 are finished; no active diagnostic process remains.

- [x] RED with meaningful counts/identity: one internally owned accepted candidate is repeatedly cloned/hashed across the real memory acceptance/read/Program path. Verify old-reader isolation, exact revision/manifest pairing, and new version after A→B→A before optimizing. Test accessor-based and exotic mutable input explicitly; a WeakSet seal must not bless changing getters or nested mutable values.
- [x] Establish plain-JSON data and ownership at the final validated candidate boundary, then deeply seal/hash that exact candidate once. Reuse this private verified identity through coordinator and memory acceptance; untrusted/unbranded inputs retain isolated copy/full validation. If postcommit code still mutates it, repair that owning boundary rather than suppressing the error or reverting to unsafe sharing.
- [x] Add an internal current-owned-snapshot handoff under existing recovery/inspection/manifest rules; public readCommittedSnapshot still returns a detached clone. Context preparation must reuse only verified owned data, validate revision and compatibility/projection rules, and seal its own untrusted clone before repeated hash-based validations. A supplied revision, shallow freeze or public boolean is never ownership proof.
- [x] Preserve identity into Agent/Program/projection consumers where they observe the same accepted version. Test new accepts, stale conflicts/rejection, batch/source/effects, old-reader immutability, rollback, cold/untrusted input, shallow freeze, nested/accessor/unsupported mutable values, forged manifest/revision and mutation through public copies. Do not alter full Transform index/restore visibility in this task.
- [x] Run focused RED/GREEN then directly affected context/memory/transaction/Program/public suites, with backups disabled and new fixtures retained. Report actual hash/clone work reduction versus remaining one-whole-world SHA256 and full traversal boundaries; root owns another immutable-source real-copy measurement and final latency verdict. Commit scoped files with task-16-report.md, no full suite/remote/production/newagents.

**Following boundary, not yet implementation:** Active/archive COW and indexes require their own exact scope. Program worldRecords already stops record recursion at the backup root; its preceding global identity/default-root validation must remain a reusable verified proof. Do not convert prepareTransformRelationIndex wholesale to active-only: ren/mov/cpy/discard/restore and reference rewrites use its complete bindings. Preserve explicit archive/restore and global duplicate-identity/multiple-root failures; unchanged-archive reuse must prove alias safety and invalidate when archive changes.

### Task 17: Reuse sealed source and unchanged archive validation (I3/U3/D3/E2)

**Evidence/dependency:** Task16 ownership is reviewed;0272922 real-copy writes remain2.40–2.80s and subsequentreads~1s. Root and retained interaction agent inspected ordinary situation source snapshots and default-backup collection: sourceAtoms is deep-cloned before/after source acceptance, while context preparation and Program records independently repeat archive key parsing despite no archive changes. This is the next narrowly measured latency task after the active Task11 implementation, not permission to redesign all indexes or disk revisions.

**Scope:** `engine.mjs` ordinary active situation source snapshots; private immutable proof/reuse in `default-backup-boundary.mjs`, and its context/Program consumers only as needed; focused tests. Preserve Task16 seal/handoff and all Task11 response/lifecycle work. No global Transform/Explore index rewrite, disk format, worker framework, production switch or external writes.

**Root bounded cost isolation,0272922:** One read-only Node evaluation on the retained qO8AGL baseline(14,688,182bytes), importing immutable0272922 modules, measured unsealed flat hash79.577ms, initial plain-JSON seal+hash115.330ms, same sealed hash reuse0.066ms, boundary collection107.805ms and immediate repeated collection83.329ms. Exit0,1.010s, no facts output/files written. This single warm-process diagnostic is not p50/p95 or a controlled whole-request comparison; it confirms each repeated boundary pass is material and that one flat hash alone does not explain the2.4–2.8s write. Task17 scope remains unchanged; final end-to-end measurements determine further work.

**Task17 active:** Original interaction agent resumed at BASE51381598bbb5038a7eadf72fbfcc955a42c78a05 after Task11 review closed. Exact19-line task17brief plus preserved ownership interfaces supplied; no session-history dump. Root owns dirty plan/requirement ledger and later real-copy measurements. No other implementation agent active.

- [ ] RED for the actual ordinary source path: source acceptance and subsequent no-effect/effect processing currently discard unchanged sealed archive identity or re-parse its facts. Assert old source/reader isolation, no alias mutation through subsequent Program effects, conflict/rejection behavior and public detached copies. Retain unsealed/disk fallback semantics.
- [ ] Use the already sealed source world where semantically equivalent before source acceptance, and the exact accepted sealed candidate afterward. Copy only when no ownership proof exists; do not freeze mutable candidates early merely to activate an unrelated all-world access-index walk. Actual Program effects keep their existing independent candidate/atomic-commit isolation.
- [ ] Keep verified archive metadata privately reusable by immutable object identity and its path/typed-boundary/topology context. A later ordinary COW candidate may reuse unchanged validated archive proof while rebuilding affected active entries. Public Maps/Sets/entry arrays must not expose mutable shared cache state. Public collect results remain compatible and complete enough for archived selector resolution; old readers stay valid. Do not treat Object.freeze of a Map, shallow freeze or caller flag as proof.
- [ ] Preserve global duplicateidentity and multipledefaultbackup validation including active-versus-archive collisions, name/nearest-domain ambiguity, malformed cold payload tolerance, typed-boundary recognition, archive-root identity/path changes and restore visibility. Structural/restore/archive edits and untrusted or legacy-normalized candidates invalidate affected proof or take the existing full validation route. Cache must not pin abandoned versions through an unbounded strong registry.
- [ ] Prove reduced repeated archive work with meaningful content-free traversal/parse evidence using existing diagnostic conventions or focused operation counts; identity equality alone is not a traversal-cost proof. Cover mutation of publicly returned Maps/Sets/name arrays without corrupting future boundary decisions. Run minimal affected boundary/context/Program/Transform tests with backups disabled and artifacts retained; independent review then root real-copy measurement, no full suite.
- [ ] Commit scoped files and `.superpowers/sdd/2026-09-16-memory-authoritative-world-save/task-17-report.md`; state remaining full-world hash/validation/index costs explicitly. No latency completion or zero-archive-compute claim without the measured end-to-end result.

**Ruling: retain exact revision bytes in this step** — the existing afterRevision is the SHA-256 commitment consumed by receipt, CAS, compatibility and Program worldKey; local changed-path evidence cannot mathematically derive that flat hash. Deferring it until after acknowledgement would silently change the contract. Task17 does not do that. The specification's no-full-hash waiting-chain target remains unmet and needs an explicit compatible proof/scheduling design if still load-bearing after measured duplicatework removal; no blanket completion exception is granted. Cost if this boundary is insufficient: another measured architectural step, not weakened receipt verification.

**Retained architecture consultation:** Confirms the flatcontentidentity contract spans world-revision, memoryauthority, coordinator inversepatch, manifest and ProgramworldKey. Strictly removing its lastfull-world dependency fromacknowledgement would require an explicit revisionidentity migration(incrementallyverifiableidentity or stabletransactionversion+integrityproof),including restart/idempotency andoldcode mapping; merely deferring SHA while preserving its oldlabel is invalid. This is not authorized as a hidden Task17optimization and no protocolmigration is implemented. Finalmeasurement must separate savingwait, serialization/hash, requiredvalidation andotherCPU; provingreadsduringsave alone does not excuse seconds-longfront-endwork or satisfy the strictertarget.

### Task 18: Preserve save-state visibility in the public CLI (I3/U2/D1/E2)

**Scope/dependency:** Existing `work-engine/atom-language/cli.mjs` result formatting and narrowly affected CLI tests, after Task11 state and Task15 pressure contract settle. This closes Task5's existing CLI acceptance obligation; do not change command grammar, HTTP contract, runtime writes or stdout Graph-JSON.

**Ruling:** A memory-mode result containing saveState emits one content-free structured status line on stderr, using the existing diagnostics output path so one-shot and interactive sessions share it. Leave stdout byte-compatible for automation, and emit no new line for disk-mode results without saveState. Successful memory acceptance remains exit0 even while saving is pending or failed; make failure visible without relabeling the accepted command as rejected. Cost if wrong: noisy CLI diagnostics or a durability misunderstanding; exact output and pending/failure/replay cases gate the change.

- [ ] RED through runAtomCli for an endpoint-shaped memory result: pending and saved state currently disappear. Cover the one-shot path and existing interactive output route without bypassing the formatter.
- [ ] Add the shared status output with accepted/saved versions and revisions, explicit pending, failure code and auxiliary-failure code; retain any already-defined content-free pressure fields only via their documented shape. Whitelist status fields, never dump facts, outcome bodies or arbitrary error objects.
- [ ] Verify equal accepted/saved content revisions can still have pending Program outcome work; do not derive pending from hash equality. Cover caught-up, pending, save-failed and replayed fresh state, unchanged disk-mode output and unchanged stdout/exit-code contracts.
- [ ] Run only directly affected CLI/state tests with backups disabled and fixtures retained; report task-18-report.md, commit scoped files and obtain independent scoped review. No full suite, production changes or new agents from the implementer.
