# Browser vendor bundles

`spatial-markdown-editor.bundle.js` is generated from
`src/spatial-markdown-editor.mjs` by `npm run build:browser`.

The bundle keeps CodeMirror 6 and Markdown rendering local so the application
does not depend on a runtime CDN.
