# `spatial-ui-paradigm` Skill 蓝图

## 目标

把本次研究固化为一个可复用 Codex Skill，使后续 HTML 系统可以在不重写业务脚本的前提下，统一接入开放式 3D 视觉交互层。多球域是当前验证载体，不是 Skill 对视觉实体、范式数量或未来项目形态的限制。

当前默认范式把所有载体球统一为可进入的隧洞；有子节点时可同层展开已有内容，无子节点时进入空域并可继续新增节点。球面显示是独立观察层，不能替代子域拓扑。它只发出静默、设备无关的视觉意图，由可配置预设映射。稀疏曲线链只采用提供的父子或显式视觉关系，绝不推断业务或流程。逻辑递归可无限，但可见实例、预取与历史必须有界。未来数据侧功能球仍可由另一个脚本域注册；本 Skill 的视觉导航域不实现数据流。

## 触发条件

当用户提出以下任一目标时启用：

- 把表格、卡片、目录、流程页改成 3D 空间范式；
- 建立球域、节点网络、空间工作台或递归门户；
- 为现有 HTML 增加设备无关的空间交互层；
- 统一多个项目的 3D 视觉交互规范。

## 明确不做

- 不修改审批、表单、权限、数据库、状态机等业务流转；
- 不替外部脚本决定一个意图是否应该写数据；
- 不把鼠标左键、右键或某个硬件按钮写死为功能；
- 不把候选、聚焦、预览、确认和操纵压缩成一个不可配置的“点击”；
- 不把 2D 卡片简单贴到 3D 背景上；
- 不用太阳系科普、行星纹理或颜色分类冒充数据结构。
- 不把当前七个验证种子固化成范式上限或互斥节点类型；
- 不把退出父域、视角后退／前进、业务撤销／重做混为一套动作；
- 不以“逻辑无限递归”为理由无限渲染、无限缓存或无限保存历史。
- 不把数据侧功能球误判为被禁止：它们由独立脚本域注册，视觉导航域不实现其数据流。

## Skill 输入

- 现有页面路径；
- 需要空间化的内容层级；
- 希望优先试验的范式；
- 当前可用设备；
- 可选的按键预设；
- 是否允许改动 HTML 结构。若不允许，则只加隔离实验层。

## 必须先建立的边界

```text
业务脚本 owns 数据、计算、权限、保存与流转
空间 Skill owns 相机、视觉实体、候选、焦点、预览、空间视图与意图
二者只通过事件契约相遇
```

默认事件：

```js
window.dispatchEvent(new CustomEvent("spatial-visual-intent", {
  detail: {
    intent: "enter",
    phase: "commit",
    targetId: "sphere-17",
    depth: 3,
    path: "root/a/b",
    visualMeta: null
  }
}));
```

`phase` 描述视觉交互阶段，不代表业务状态。`visualMeta` 只允许视角位移、缩放量、祖先深度和抓取阶段等白名单视觉参数，禁止任意载荷。即使外部脚本订阅 `commit`，也不能把它自动解释成“提交表单”或“审批通过”。外部脚本必须自行验证、决定和执行，空间层只等待结果并更新视觉反馈。

## 必须具备的空间语法

- 开放视觉实体：以 `Shape + Boundary + Presence + Capabilities + LocalFrame` 组合，不限定七种范式；
- 统一隧洞载体：所有球体均可进入；有子内容可同层展收，空隧洞进入后可新增节点；独立球面显示不替代子域拓扑；
- 开放视觉注册：实体、能力、预算和命令必须通过白名单注册表接入，业务字段与非视觉意图必须被拒绝；
- 四级语义尺度：`beacon → identity → preview → interior`，边界必须带滞回；
- 交互阶段：`candidate / focus / commit / manipulate` 为直接路径，`preview` 只是设备或任务需要时的可选兼容阶段；任何临时阶段都能取消；
- 隧洞与球面分工：隧洞只表达可穿越纵深，不在内部缩绘子拓扑；独立球面不替代展开或进入；
- 域径图：从全域向下显示进入路径、当前层与可返回深度，不退化为二维小地图卡；
- 细节镜：支持比较但默认最多两个，并有明确回收规则；
- 子域背景：用大尺度半透明隧洞轮廓和入口名维持当前域上下文；
- 视觉历史：`backView / forwardView` 与 `exit / exitToDepth` 分离；
- Focus+Context：焦点、直接关系、祖先、同级、远端和工具按权重分配视觉预算；
- 关系约束：父子用低亮虚线，同层／显式关系用较亮实线和代表性文字，只表达提供的视觉关系，不推断业务、流程或箭头；
- 选择歧义：命中先构造候选集并评分，低置信度不冒充唯一答案；
- 有界生命周期：活动节点、递归层、镜体、历史、路径、预取和域缓存都必须声明上限或回收策略。
- 手势仲裁：按下只产生候选，位移阈值区分点击、环绕与抓取，松手才确认；必须允许从节点表面起手旋转。

## 执行流程

1. 只读检查现有 HTML、CSS 与业务脚本边界。
2. 建立独立实验目录；默认不覆盖生产页面。
3. 提炼内容层级，但不解释或改写业务规则。
4. 选择足以验证假设的最小实体组合；数量不硬设为两至七种，也不被“实体／空体／镜体”限制。
5. 建立 `spatial-entity-registry.js`，用视觉字段、能力、预算和命令白名单开放注册扩展点。
6. 建立 `spatial-view-grammar.js`，先定义语义尺度、滞回、交互阶段、视觉参数白名单、Focus+Context 和视觉快照，再实现外观。
7. 生成输入映射、意图词汇、手势仲裁和可组合实体配置，确保按键配置与功能语义分离。
8. 实现统一隧洞、空域进入、独立球面、同层展收、使用／游走分离、曲线关系、局部球域、域径图与惰性渲染。
9. 实现并区分退出父域、返回全域、视角后退和视角前进。
10. 为候选遍历、活动节点、递归展开、镜体、历史、路径和域缓存设置预算与回收策略。
11. 实际渲染截图并检查第一眼是否仍像卡片网页；未渲染前不得声称视觉验收通过。
12. 实测手势仲裁、语义滞回、左键聚焦／清焦／使用、右键单球／全域展收、右键双击进退、中键球面、域径返回、视角历史和低置信度候选。
13. 用故意夹带的业务字段与非视觉意图攻击注册表和事件契约，确认白名单拒绝。
14. 输出迁移边界、事件契约、预算清单、验证证据与未触碰脚本清单。

## 推荐文件模板

```text
spatial-layer/
  index.html
  tokens.css
  spatial.css
  input-config.js
  spatial-entity-registry.js
  spatial-view-grammar.js
  spatial-engine.js
  SPATIAL-GRAMMAR.md
  README.md
  screenshots/
```

## 载体配置形状

```js
const entity = {
  label: "新的视觉载体",
  short: "NEW",
  description: "只描述其空间用途。",
  preview: "orbit",
  position: { x: 2, y: 1, z: -3 },
  radius: 0.9,
  capabilities: {
    body: true,
    portal: true,
    lens: true,
    halo: true,
    focusField: true,
    satellites: true,
    grabbable: true
  },
  preferredIntent: "reveal",
  visualBudget: {
    importance: 1,
    renderCost: 1,
    labelPriority: 0.7,
    maxSatellites: 6,
    maxVisibleDepth: 3
  }
};

SpatialEntityRegistry.registerDefinition("new-visual-carrier", entity, { root: true });
```

当前 MVP 的主渲染器仍以半透明球为基础载体，但注册数量和能力组合已经开放。`preferredIntent` 是场景默认视觉行为，不是按键绑定；物理输入只能在独立映射中引用它。以后增加新几何表现时，应新增受限的渲染键，而不是允许注册表执行任意回调。

## 交付验收

- 页面第一眼是空间场，不是“星空背景 + 卡片”。
- 没有中心太阳、行星拟真纹理或业务颜色分类。
- 当前主载体至少支持候选、聚焦与一种直接局部行为；可选兼容预览不作为高频操作前置条件，也不要求所有未来实体都是球。
- 四级语义尺度切换稳定，阈值附近没有反复闪烁。
- 隧洞不缩绘子拓扑；使用、同层展开、沉浸进退和中键球面彼此不交叉。
- 可以进入和退出，域径图可见且能退出到指定深度。
- 子域背景和域径图能在局部恢复全域方向，细节镜最多两个且可回收。
- 视角后退／前进不等于退出父域，业务撤销／重做不由空间层处理。
- 密集命中使用候选评分，低置信度有继续精化的位置。
- 逻辑深度与渲染数量分离。
- 所有无限逻辑都有明确活动预算或缓存上限。
- 换输入预设后不用修改球体或相机逻辑。
- 从球体表面拖动可环绕，未越阈值的松手才确认；抓取由独立映射明确触发。
- 新实体或命令可通过视觉注册表加入，未知字段和非视觉意图会被拒绝。
- 数据侧功能球可由独立脚本域注册，当前视觉导航域不实现数据流。
- 对外只发视觉意图，不直接写业务数据。
- 至少保存一张全域截图和一张交互态截图。

## 后续封装建议

真正创建 Skill 时，把规则拆为：

- `SKILL.md`：触发、边界、执行流程和验收；
- `references/intent-vocabulary.md`：完整意图词汇；
- `references/spatial-grammar.md`：语义尺度、状态机、历史和 Focus+Context；
- `references/paradigms.md`：开放式空间实体与代表范式，不设固定数量；
- `references/research-sources.md`：只保留原始论文、标准和官方平台文档；
- `references/visual-system.md`：深空视觉纪律；
- `assets/starter/`：本 MVP 的无业务脚本模板；
- `scripts/verify-spatial.js`：检查映射分离、事件契约、语义滞回、历史分离、预算、递归和视口。

等范式经过两到三个真实项目验证后再创建正式 Skill，避免把第一版探索误固化成僵硬标准。
