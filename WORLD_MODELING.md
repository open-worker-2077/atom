# World Modeling（世界建模）

## 定义

**World Modeling** 是一种由人和 Agent 共同构造、运行、观察并更新世界表征的总范式。需要强调可执行边界时，可称为 **Executable World Modeling**。

它不只训练一个预测世界的 AI 模型，而是共同维护：

- 世界在过去、现在或未来如何被表征；
- 信号如何进入世界；
- 函数如何加工信号与数据；
- 旧状态如何转变为新状态；
- 结果如何被感知或执行。

它与当前 AI 行业中的 World Model 有明确区别：

- **World Model** 是用于理解环境、预测变化和支持规划的模型；
- **World Modeling** 是组织 Logic、Data、交互与运行过程的总范式；
- World Model、脚本和大模型 API 都可以成为 World Modeling 中的函数节点；
- **World Coding** 保留为其中“人工或 Agent 编写可执行规则”的技术子范式。

本文记录探索方向，不表示当前 `graph-4d` 视觉引擎已经承担业务计算或数据流。

## 一个世界，两份 JSON

一个世界在物理上放进同一个目录：

```text
world/
├─ world.logic.json
└─ world.data.json
```

- `world.logic.json` 是规则图，进入 GitHub。
- `world.data.json` 是表征图，默认留在本机。
- 两个文件逻辑上共同构成一个世界。

当前阶段使用“规则”而非“规律”：这些逻辑主要由人或 Agent 编写。未来经由学习、组合与涌现形成的规则，才可能逐渐逼近自然规律。

## Logic 图

### Logic 节点

Logic 节点是函数。函数只声明自己接收什么、输出什么，不指定输入来自哪个上游函数。

函数内部可以采用两种执行方式：

- **Script 函数**：使用 Python、JavaScript 等脚本完成逻辑推理或数学运算；
- **Model 函数**：调用大模型 API，以 JSON 为输入和输出。

函数定义与函数调用是一体两面：

- 人工编写时，节点呈现函数定义；
- 自动运行时，同一个节点接受信号并执行函数；
- 每次执行只是运行记录，不生成临时节点。

### Logic 边

Logic 边把一个函数的出参连接到另一个函数的入参。

节点保持独立和可复用，边负责决定本次世界中“谁连接给谁”。Logic 边不计算、不存储，只承担明确的信号路径。

## 节点化编程与脚本生成

World Coding 是 World Modeling 中用于学习和手工编程的中间层。

Agent 可以把常用脚本知识封装成可调用的基础函数节点。每个节点公开：

- 用途说明；
- 固定入参；
- 固定出参；
- Script 或 Model 实现；
- 可检查的运行示例与错误。

人工编程时，不必先记住完整的 Python 或 JavaScript 语法，可以先选择、连接和组合基础节点，形成 `world.logic.json`。随后由确定性的代码生成器把 Logic 图翻译成可读的 `.py`、`.js` 等脚本文件。

```text
基础函数知识
      ↓ Agent 封装
可调用函数节点
      ↓ 人工连接
world.logic.json
      ↓ 确定性生成
.py / .js
```

该过程采用单向真相源：

- `world.logic.json` 是可编辑的源定义；
- `.py`、`.js` 是生成产物；
- 生成文件默认不手动修改；
- 同一函数被多处使用时只保留一份定义，其余节点引用该定义；
- “一个函数节点”不强制等于“一个物理脚本文件”，代码生成器可以按模块聚合函数，避免文件数量爆炸。

未来若要支持传统 IDE 修改脚本后反向同步，必须通过语法树解析形成受控导入流程；首个 MVP 不承担无约束的双向同步。

## Logic 学习与改写

人会根据经验修改自己的规则，Agent 也可以学习并提出对 `world.logic.json` 的改写。但 Logic 改写不同于普通 Data 更新：

- 普通运行只读取 Logic，并通过 JSON Patch 更新 Data；
- 学习模式可以生成候选 Logic Patch；
- 候选改写必须记录来源、理由、测试和影响范围；
- 通过人工确认或约定的验证门后，候选规则才能成为当前规则；
- 首个 MVP 禁止 Agent 静默改写自己的 Logic。

因此，自我学习不是随意修改自身，而是一个可观察、可验证、可回退的规则演化过程。

## Data 图

### Data 节点

Data 节点保存事物、数值、状态或事件的当前表征。

### Data 边

Data 边也是数据，用于表达表征之间真实存在的关系，例如：

```text
旧表征 ──演化为──> 新表征
事物 ──位于──> 空间
事件 ──发生于──> 时间
```

Data 图会作为 JSON 输入进入 Logic 图，并接收 Logic 图输出的新节点、新边或局部变化。

## 状态转移

World Modeling 的基本运行式是：

```text
Logic(Dataₜ, Signal) → Patch → Dataₜ₊₁
```

含义是：

1. 感知交互产生一个 JSON 信号；
2. Logic 图中的函数按边传递和加工 JSON；
3. 函数输出 Data 图需要发生的变化；
4. 系统应用变化，形成新的 Data 图；
5. 执行交互把结果传到世界之外。

状态保存在 Data 图中，Logic 图只定义状态如何转移。

## 理解、预测、模拟与规划

World Model 常见的四项能力可以放入同一条链路：

1. **理解**：把感知信号整理成当前世界表征，回答“现在可能是什么状态”；
2. **预测**：根据当前表征、行动和规则产生未来候选，回答“接下来可能发生什么”；
3. **模拟**：反复执行预测，在不改变现实状态的情况下展开一条或多条未来轨迹；
4. **规划**：比较不同轨迹与目标之间的差异，选择下一步行动。

```text
感知 → 理解当前状态 → 预测下一状态
                       ↓ 反复展开
                    模拟多条轨迹
                       ↓ 比较目标
                       规划行动
```

`world.data.json` 不被限制为只记录过去和现在。它可以包含未来时间段，但未来表征必须说明自己的认识状态，例如：

- `observed`：已经观察到；
- `predicted`：模型预测；
- `planned`：计划采用；
- `hypothetical`：用于模拟或反事实分析。

未来数据不是自动被视为已经发生的事实。它还应记录有效时间、生成来源和必要的置信信息。真正执行后的感知结果再进入 Data 图，与先前预测进行比较。

## 身份与查询

World Modeling 不要求用户记忆或填写随机编号。人通常按照时间、空间、语义和关系回忆事物，系统查询也应以表征为主：

- 属性查询；
- 时间查询；
- 空间查询；
- 图关系查询；
- 向量相似查询。

系统内部仍需要精确地址，让边能够引用节点。该地址只是隐藏的定位工具，不宣称事物拥有永恒不变的哲学身份。

当前身份方向是：

- **内容指纹**用于精确引用、完整性检查和去重；
- **时空区位**区分一次具体发生；
- **向量表征**用于寻找相似事物；
- **Data 边**记录旧表征如何演化为新表征。

内容指纹的具体算法不进入首个 MVP。

## 借鉴的现有规范

World Modeling 不从零创造所有 JSON 语法，优先组合已有标准：

- [JSON Schema](https://json-schema.org/specification)：检查 JSON 是否符合规定结构；
- [JSON Pointer](https://www.rfc-editor.org/info/rfc6901/)：定位 JSON 文档内部内容；
- [JSON Patch](https://www.rfc-editor.org/info/rfc6902/)：表达增加、删除、替换等状态变化；
- [JSON-LD](https://www.w3.org/TR/json-ld/)：表达图节点、关系、类型与引用；
- Git 内容寻址：作为未来内容指纹机制的参考。

JSON Schema 位于 World 语法下层。JSON-LD 与 JSON Patch 会直接影响 World 语法本体。首个 MVP 只采用完成闭环所需的最小子集。

## 外周交互

交互节点连接 World 与外部环境：

- 感知方向：把表单、变量、设备或其他外界变化转换为 JSON 信号；
- 执行方向：接收 JSON 结果并显示文字或驱动外部执行；
- 双向交互：同时具备感知与执行端口。

感知与执行只是交互方向，不承担业务推理。业务加工始终属于 Logic 函数。

## 与 graph-4d 的边界

`graph-4d` 当前仍是视觉交互与本地知识图谱试验场：

- 3D 引擎负责空间呈现、命中、视角、展开、进入和视觉意图；
- World Modeling 运行时负责函数执行、Data 状态转移和外周交互；
- 视觉引擎可以广播意图并观察运行结果；
- World Modeling 不应把业务状态机写进 `spatial-engine.js`；
- 两者未来通过明确的适配层连接。

## 首个 MVP

首个 MVP 只验证一条最小信息系统闭环：

```text
数字表单
   ↓ JSON
四则运算 Script 函数
   ↓ JSON Patch
Data 图保存输入与结果
   ↓ JSON
文字输出
```

示例：

```text
输入 5 → 计算 (5 + 3) × 2 → 保存变化 → 显示 16
```

首个 MVP 必须证明：

1. `world.logic.json` 能定义函数节点与端口连接；
2. `world.data.json` 能保存 Data 节点和 Data 边；
3. 表单能产生 JSON 信号；
4. Script 函数能接收 JSON 并输出 JSON；
5. JSON Patch 能把旧 Data 状态更新为新状态；
6. 文字输出能显示最终结果；
7. 每一步输入、输出、变化和错误都可检查。

首个 MVP 暂不包含：

- 大模型 API；
- 并发、循环和复杂调度；
- 自动涌现；
- 内容指纹算法；
- 数据库服务；
- 外接硬件；
- 对现有 3D 视觉交互的改造。

首个 MVP 可以在闭环跑通后增加一个不改变运行语义的验证：把四则运算 Logic 图生成一份可直接阅读和执行的脚本文件，以证明“节点搭建 → JSON → 原生脚本”的路径成立。

## 尚待确定

MVP 开始前只需继续确定六件事：

1. `world.logic.json` 采用哪些 JSON-LD 字段；
2. Logic 节点入参、出参和函数体的最小结构；
3. `world.data.json` 中 Data 节点与 Data 边的最小结构；
4. JSON Patch 如何连接一次函数执行与一次 Data 状态更新；
5. Data 表征如何区分已观察、预测、计划与假设；
6. Logic Patch 通过什么验证门才能成为当前规则。
