# 3D 空间视觉交互语法

## 0. 研究边界

本语法研究的是：怎样把传统页面、表格、单据和面板中的“看见、定位、选择、展开、进入、退出、比较、回溯、调整视角”改写为可组合的 3D 空间交互。

它不是一套业务系统，也不假定球体是唯一形态。球体只是当前深空原型中最适合表达无方向边界、局部场域和递归入口的一种载体。今后可以替换为点、壳、云、带、簇、曲面、体积、轨迹或其他空间结构，只要仍遵守下面的交互语义。

明确排除：

- 不定义审批、保存、提交、流转、计算、权限、同步等业务规则；
- 不直接修改业务数据，不把业务状态机藏入 3D 引擎；
- 不把鼠标左键、右键、手势或手柄按钮写成固定功能；
- 不以“把二维卡片悬浮进三维空间”作为范式创新；
- 不要求逻辑上的无限递归等于同屏无限渲染。

3D 层只维护视觉状态、空间视图、导航历史与输入意图。外部脚本可以订阅抽象意图，也可以向 3D 层提供只读快照和显示描述。

## 1. 基本句法

一个空间交互场景由四部分组成：

```text
场域 Field
  + 空间实体 Entity
  + 观察工具 Tool
  + 关系痕迹 Trace
  + 视角 View
```

任何对象都不应被永久锁死为“实体类、空体类、镜体类”。更稳定的做法是让对象组合若干视觉能力：

```text
Entity = Shape + Boundary + Presence + Capabilities + LocalFrame
```

- `Shape`：球、点、云、曲面、轨迹、簇等几何外观；
- `Boundary`：对象或子场域的空间边界；
- `Presence`：远近层级下的可见表现；
- `Capabilities`：可聚焦、可展开、可窥视、可进入、可观察、可移动等能力；
- `LocalFrame`：对象自身的局部坐标系，用于递归嵌套。

同一对象可以同时拥有外壳、入口能力、镜体能力、命令晕与子节点。当前载体球统一为可进入的隧洞；有子节点时可同层展开已有内容，无子节点时进入空域并可继续新增节点。球面显示则是独立观察层，可单独切换，绝不替代子域拓扑。所谓“一球多面”应理解为能力组合，不是互斥类型切换。

## 2. 空间实体与观察工具

### 2.1 场域 `Field`

当前可观察和可操作的局部世界。场域没有永久中心；焦点只是在某一时刻被提升为视觉中心。场域可包含其他场域的入口，也可被上级场域表示为一个代理实体。

### 2.2 载体 `Carrier`

承载身份和可交互能力的空间实体。当前原型用半透明球体，但语法只要求它有可辨认边界、空间位置和稳定身份，不要求模拟真实行星。

### 2.3 簇 `Cluster`

多个载体的暂时聚合表现。靠近、聚焦或展开后，簇可拆解为相对位置稳定的成员；离开焦点后重新聚合，以控制复杂度。

### 2.4 入口 `Portal`

把另一个局部场域映射到当前场域中的可穿越边界。入口可以是球形隧洞、壳面、裂隙或其他闭合空间，不应退化为矩形门或网页弹窗。

### 2.5 镜体 `Lens`

只改变局部区域“怎样被看见”的观察工具，不改变对象本身。镜体可放大、透视、剖切、隔离、比较或显示另一尺度。它应有明确影响体积和可见边界。

### 2.6 全域镜 `WorldLens`

当前域或祖先域的微缩动态视口。它在进入局部以后仍提供全局方向、远距选择和返回入口，但不常驻为二维小地图卡片。它可以悬浮、固定到空间锚或随时召回。

### 2.7 锚 `Anchor`

稳定的视觉参照物，包括当前焦点、祖先代理、用户固定的视角标记和临时比较对象。锚的职责是保持方位和对象恒常性，不承担业务保存。

### 2.8 命令晕 `Halo`

围绕当前锚展开的空间命令节点。菜单属于对象的局部空间，不属于屏幕边缘。命令晕发出抽象意图；具体输入由配置模块绑定。当前原型将它保留为兼容／调试入口，而非核心导航路径。

### 2.9 轨迹 `Trail`

表达“从哪里来、当前在哪、还能往哪里去”的空间痕迹，包括祖先路径、视角历史、转场残影和方向线。稀疏曲线链只表达外部提供的父子或显式视觉关系；它不是业务流程图，不推断流程、职责或箭头语义。

### 2.10 幽灵代理 `Proxy`

未实际展开或已离开渲染窗口的对象代理。它保留身份、方向、层级和可返回性，同时避免实例化完整内容。

## 3. 核心交互状态机

状态描述的是视觉关系，不描述物理按键。典型路径为：

```text
rest
  → candidate
  → anchor
  → direct intent / optional peek
  → transition
  → stable / manipulate
```

### `candidate` 候选

射线、触点、凝视、邻近或键盘遍历命中对象后产生的短暂状态。只提供克制的轮廓、深度和身份提示，不立即展开菜单或改变视角。

### `anchor` 锚定

用户确认“当前关注的是这个对象”。锚定后对象获得稳定轮廓，周围关系适度显露，其他对象降权但不消失。锚定不等于执行或进入。

### `peek` 窥视

兼容性的可逆预览。它可以放大目标、加强方向或提示穿越纵深，但当前隧洞不在球内缩绘子拓扑。实际子节点只由同层展开或沉浸进入实例化；独立球面也不替代这一层级关系。松开、移开或取消即可回到锚定态。

### `commit` 提交视觉意图

确认接下来要发生进入、退出、展开、收拢、切换镜体或视角跳转。这里的“提交”只提交视觉意图，不是提交业务表单。

### `transition` 连续转场

在原视图与目标视图之间保持对象恒常性。转场必须能够看出起点、方向和目标；远距跳转宜先退远到同时容纳两端，再靠近目标，而非瞬间换页。

### `stable` 稳定观察

转场完成后的新局部场域或新视角。递归域径、视角历史和返回路径仍然存在。

### `manipulate` 空间操作

抓取、移动、旋转、缩放、固定、比较等视觉布局操作。默认只改变个人视图状态；需要写回业务数据时，必须另行向外部脚本发出意图，由脚本决定是否执行。

状态机允许短路：例如普通载体可以从 `anchor` 直接进入 `manipulate`，入口可以由直接意图进入 `transition`，也可在其他输入设备上插入可逆 `peek`。任何临时阶段都必须支持 `cancel` 回到最近稳定状态。

## 4. 语义缩放

缩放不只是把同一模型放大，而是把尺度本身变成信息选择器。推荐五级表现：

| 层级 | 视觉表现 | 允许动作 |
| --- | --- | --- |
| 信标 `beacon` | 点、微光、方向与密度 | 定向、候选 |
| 身份 `identity` | 轮廓、名称或稳定符号 | 锚定、切换焦点 |
| 预览 `preview` | 子结构暗示、入口深度、少量关系 | 窥视、展开 |
| 结构 `structure` | 卫星、局部网络、镜体内容 | 比较、空间操作 |
| 场域 `inhabit` | 对象成为当前可进入世界 | 局部导航、继续递归 |

实现要求：

- 层级依据投影尺寸、观察距离、焦点权重和视图复杂度共同决定；
- 临界值应带滞回，避免靠近阈值时反复闪烁；
- 远处对象先改变表现复杂度，再决定是否停止渲染；
- 聚焦对象可以比同距离对象提前进入更高语义层；
- “进入场域”应是语义缩放的自然延续，而非打开新页面。

## 5. 入口与球形隧洞

入口的直接视觉阶段建议为：

```text
远距暗示 → 边界显形 → 方向提示（可选） → 连续穿越 → 新域稳定
```

关键规则：

- 入口始终是可穿越的纵深边界，不在内部缩绘或泄露子域拓扑；
- 通过视差、内层稀疏标记和亮度衰减表达“深不见底”，不依赖高速粒子；
- 入口外沿必须稳定，使用户能判断边界、朝向和距离；
- 进入前显示目标域的主要方向锚，进入后保留父域代理；
- 入口与出口的观察方向尽量连续，避免无提示的朝向翻转；
- 递归深度逻辑上不设上限，但同屏只预取一个即将进入的子域；
- 当前 MVP 在短暂穿越期间锁定冲突输入并保证完成；沿原轨迹中途取消属于后续扩展要求。

## 6. 焦点与全域上下文

3D 空间不等于把所有节点同时摊开。焦点变化时应同时发生三件事：

1. 目标向易观察区域移动或获得更高视觉权重；
2. 直接邻域、父路径和相关入口适度展开；
3. 外围对象被压缩、降亮或代理化，但保留方向和拓扑。

这是一种连续的 Focus+Context 场，不要求第一版真正采用双曲几何。当前 MVP 可以先用距离、焦点权重、透明度和投影尺寸近似；后续再替换空间布局算法，而不改变交互语法。

## 7. 全域镜与局部镜

### 全域镜

- 在局部域内召回上级或全局微缩视图；
- 镜中对象与真实对象共享身份，但镜只是视图代理；
- 可从镜中锚定远距目标，随后生成可理解的转场；
- 可固定在空间中，也可关闭，不占用永久 HUD。

### 局部镜

- 影响一个明确的球形或其他封闭体积；
- 镜外保持原场景，镜内显示放大、透视、剖面或替代编码；
- 多镜可以并存，但 MVP 同时激活不超过两个；
- 镜体叠加必须有明确顺序或禁止组合，避免产生不可解释结果。

## 8. 祖先轨迹与视角历史

### 祖先轨迹

祖先轨迹描述当前场域在递归结构中的位置：

```text
全域 ··· 父域 ··· 当前域
```

它应以空间锚、细弱路径或远处代理表达，不做面包屑卡片。祖先越远，表现越抽象；任一祖先都可成为返回目标。

### 视角历史

视角历史与层级路径不同。用户可能在同一场域内多次旋转、缩放、比较和切换焦点，因此需要独立记录：

- 相机位置、方向和尺度；
- 当前场域路径；
- 焦点与锚；
- 语义缩放层级；
- 激活的镜体和局部展开状态。

`backView` / `forwardView` 只恢复视觉状态，不撤销业务数据。连续微小相机运动应合并为一次历史记录，进入、退出、聚焦和显著视角跳转才创建新记录。

## 9. 空间歧义的处理

遮挡、深度重合和近邻目标是 3D 选择的固有问题。当前阶段不必把它们发展成独立研究主线，但框架需要保留以下基本语法：

- `candidate` 先于 `anchor`，避免命中即执行；
- 命中体可略大于可见体，但不能越过明显的邻近对象；
- 多目标重合时显示候选深度序列，允许 `nextCandidate` / `previousCandidate` 切换；
- 锚定后用轮廓、近弱远强的深度线索和局部降亮分离目标；
- 小节点可临时拉向观察者，确认后回到原位；
- 所有不可逆视觉跳转都要有明确目标方向；`peek` 是可选兼容阶段，不是高频进退的前置菜单。

## 10. 渲染预算：逻辑无限，视窗有限

递归数据结构可以无限生成，活动渲染窗口必须严格有限。每帧优先级建议为：

1. 当前场域和当前焦点；
2. 当前焦点的直接邻域；
3. 少量可见兄弟和候选；
4. 祖先路径代理；
5. 一个即将进入的预取子域；
6. 激活的全域镜或局部镜。

预算规则：

- 超出窗口的对象保留为代理或统计信标，不创建完整几何；
- 语义层级同时承担 LOD 职责；
- 子簇离开焦点后自动收拢；
- 镜体按实际可见体积单独计入预算；
- 帧率下降时先减少外围复杂度、镜体分辨率和星点，不降低焦点反馈；
- 转场期间可暂时隐藏文字和细连接，稳定后恢复；
- 任何硬上限都应集中配置，不散落在交互逻辑中；逻辑递归可以无限，但可见实例、预取和视角历史必须有界。

## 11. 设备无关的视觉意图

范式层只识别意图，不识别鼠标按键。建议保留以下开放词汇：

| 分组 | 视觉意图 |
| --- | --- |
| 指向 | `aim`、`candidate`、`nextCandidate`、`previousCandidate` |
| 聚焦 | `anchor`、`releaseAnchor`、`nextFocus`、`previousFocus` |
| 直接操作 | `toggleChildren`、`toggleSurface`、`toggleFieldSurfaces` |
| 兼容预览 | `peek`、`endPeek`、`inspect`、`switchLens` |
| 确认 | `commit`、`confirm`、`cancel` |
| 视角 | `orbit`、`pan`、`dolly`、`roll`、`fly`、`resetView` |
| 层级 | `enter`、`exit`、`returnOverview`、`reveal`、`collapse` |
| 空间操作 | `grab`、`release`、`move`、`rotateObject`、`scaleObject`、`pin` |
| 观察工具 | `summonLens`、`summonWorldLens`、`magnify`、`compare`、`dismissTool` |
| 命令晕 | `summonMenu`、`chooseMenuItem`、`dismissMenu` |
| 历史 | `backView`、`forwardView`、`returnAnchor` |

独立输入配置模块负责把鼠标、键盘、触控、触控板、手柄、凝视、语音或手势映射到这些静默视觉意图。映射可以按设备、用户或场景更换；默认预设不是功能定义，空间状态机不改写。

## 12. 视觉层与脚本层的接口契约

### 开放注册表边界

空间实体、能力、视觉预算与命令通过 `spatial-entity-registry.js` 注册。注册只接受有限视觉字段；未知字段被丢弃，命令必须引用已知视觉意图，禁止注册任意回调、业务状态或业务动作。当前主渲染器以球体为基础，但根实体和命令数量不再固化。未来数据侧功能球并非禁止项，只能由另一个脚本域注册并自行处理数据；本视觉导航域不实现数据流。

指针输入先由手势仲裁器处理：`pointerdown` 只产生候选，位移超过阈值才进入环绕或抓取，未越阈值的 `pointerup` 才确认。因而物理按下、视觉候选和功能确认不是同一件事。

对外事件只携带白名单化的 `visualMeta`。目前可出现的参数只有环绕位移、缩放量、祖先深度、抓取阶段，以及 1–3 的最终确认击数 `confirmationCount`。击数只表明视觉层识别出的使用承载位，不等于保存、审批或任何业务动作；任意载荷、字段对象和业务状态不会穿过视觉层。

### 视觉层可以做

- 接收外部提供的节点描述、层级关系和只读显示状态；
- 维护相机、焦点、展开、镜体、布局和视角历史；
- 发出 `spatial-visual-intent` 一类抽象事件；
- 为意图附带对象身份、场域路径、视角和输入来源；
- 在外部脚本拒绝或完成动作后显示相应反馈。

### 视觉层不得做

- 根据点击自行判定审批通过、记录保存或任务流转；
- 把业务字段、权限判断和计算规则写入节点类型；
- 将空间布局变化默认为业务数据写回；
- 把“进入子域”与“打开某个固定业务页面”永久绑定；
- 用 3D 动画掩盖脚本失败或未确认状态。

推荐接口形态：

```text
输入设备
  → 输入映射
  → 视觉意图
  → 3D 状态机与表现
  → 可选的外部意图事件
  → 业务脚本自行决定是否处理
```

反向更新：

```text
业务脚本提供显示快照
  → 视觉层更新对象表现
  → 不推断业务规则
```

## 13. MVP 验证状态

当前版本已经打通四条组合路径；后续迭代继续沿用同一验收顺序：

1. `candidate → anchor → semantic zoom → structure`：验证从远距信标自然过渡到局部结构；
2. `anchor → enter → portal transition`：验证直接向前进入、空域直接向后退出，并恢复同一入口；
3. `inhabit → summonWorldLens → anchor remote → transition`：验证局部中仍能掌握全域；
4. `enter → enter → backView / ancestor anchor → exit`：验证递归深度与视角历史互不混淆。

验收应记录意图次数、到达时间、返回成功率、方向判断和同屏有效上下文，而不只评价“像不像宇宙”。

## 14. 研究依据

- Bederson 与 Hollan 的 [Pad++](https://hci.ucsd.edu/hollan/Pubs/JH1995-1.pdf) 将语义缩放定义为对象随尺寸、视图复杂度和任务改变表现，并用连续缩放转场维持对象恒常性。
- Munzner 的 [H3](https://graphics.stanford.edu/papers/h3/html/h3.htm) 展示了 3D 双曲空间中的 Focus+Context、子树展开、裁剪和大规模层级浏览。
- Feiner 与 Beshers 的 [Worlds within Worlds](https://www.cs.columbia.edu/~feiner/courses/csw4172/resources/Feiner-Beshers-UIST1990.pdf) 提供了任意变换、递归嵌套局部坐标世界的早期交互模型。
- Stoakley、Conway 与 Pausch 的 [Virtual Reality on a WIM](https://www.cs.cmu.edu/~stage3/publications/95/conferences/chi/paper.html) 说明全尺寸视角与微缩动态视口可以并存，并可统一支持选择、导航与观察。
- Viega 等人的 [3D Magic Lenses](https://www.cs.cmu.edu/~stage3/publications/96/conferences/uist/lenses/index.html) 区分了 3D 平面镜与体积镜，并指出局部预览、上下文保持和多镜组合的可能性及成本。
- Han、Kim 与 Cho 的 [PORTAL](https://digitalcommons.usu.edu/computer_science_stures/26/) 将入口作为远距目标的观察与交互窗口，支持“先透过入口作用，再决定是否移动视角”的设计方向。
- Bowman 与 Wingrave 的 [沉浸式菜单对比研究](https://eprints.cs.vt.edu/archive/00000544/) 表明速度、偏好和不适感并不总是一致，支持把命令语义与物理设备映射分离。
- Robertson、Mackinlay 与 Card 的 [Cone Trees](https://doi.org/10.1145/108844.108883) 使用连续 3D 动画重排层级焦点，说明转场本身可以承担认知衔接。

## 15. 开放性原则

这份语法不是最终的节点分类表，而是一套可扩展的组合约束。新增视觉实体或交互方式时，只需回答：

1. 它属于场域、实体、工具、痕迹还是视角？
2. 它在哪些状态出现，怎样取消和回到稳定态？
3. 它如何随语义尺度改变？
4. 它是否保留了祖先、方向和对象恒常性？
5. 它占用多少活动渲染预算？
6. 它发出的是否仍是设备无关视觉意图？
7. 它是否越界承担了业务数据流？

只要第七项为否，其余六项可被明确回答，这个新元素就可以进入后续原型试验。
