# Task 17 report — sealed source and unchanged archive proof

Base: `51381598bbb5038a7eadf72fbfcc955a42c78a05` (interleaved root documentation commits excluded from this source scope).

## Change and evidence

- Ordinary single-situation source processing uses the prior sealed source for a no-change edit, and the exact accepted sealed candidate after a changed source commit. Unsealed/disk paths still copy. The real-memory fixture retains an unchanged archive subtree, old reader, independent save/restart recovery and public detached snapshot checks. Instrumenting `structuredClone` by exact accepted-candidate input established **2 calls before, 0 after**.
- The boundary retains a private validated proof for one typed default-backup subtree keyed by its sealed root identity and exact path. On a later sealed COW version, it traverses/rebuilds active entries, verifies active identities against the archived identity map, and uses the proof for archived path/name/identity/inactive lookups. A structural/path/type change, second backup root, duplicate path or identity collision takes the original complete-validation route. The context and Program consumers use a read-only internal view; the public collector still returns detached native Maps/Sets/name arrays. WeakMap ownership does not pin abandoned versions through a strong registry.
- A content-free `ATOM_PERF_TRACE=1` boundary event reports visited and proof-reused Atom counts. The focused 300-descendant fixture records a candidate with **2 visited Atoms and 301 proof-reused entries**; before this change no reuse event existed. This counts the boundary traversal only. It does not mean zero archive work globally: initial proof construction, public full materialization, seal/hash and other consumers remain.

## TDD and verification

- RED: `ATOM_RUNTIME_BACKUP_REPO='' node --test --test-isolation=none --test-name-pattern='real engine reuses an accepted memory version' tests/atom-memory-authoritative-interaction.test.mjs` → 0/1, accepted candidate clone count `2 !== 0`. GREEN: same command → 1/1. New fixture retained (no new deletion hook).
- RED: `ATOM_RUNTIME_BACKUP_REPO='' node --test --test-isolation=none --test-name-pattern='sealed ordinary edits reuse private archive proof' tests/atom-default-backup-active-boundary.test.mjs` → 0/1, no qualifying reduced-traversal event. GREEN: same command → 1/1.
- Final boundary/context/real-memory command: `ATOM_RUNTIME_BACKUP_REPO='' node --test --test-isolation=none tests/atom-default-backup-active-boundary.test.mjs tests/atom-language-context-store.test.mjs tests/atom-memory-authoritative-interaction.test.mjs` → **35/35**, exit 0. The first non-escalated context run had only sandbox `EPERM` creating OS temporary fixtures; the authorized rerun passed.
- Directly affected Transform/Program/projection command: `ATOM_RUNTIME_BACKUP_REPO='' node --test --test-isolation=none tests/atom-transform-postcommit-boundary.test.mjs tests/atom-program-runtime-scheduling.test.mjs tests/atom-language-transform-batch.test.mjs tests/atom-projection-pipeline.test.mjs` → 150/152; all Transform and projection cases passed. Two old Program assertions failed: frozen accessor read count `17 !== 12`, and unsealed shallow-frozen 10k-world cached fingerprint `155–530ms > 30ms`. Root reproduced those exact two failures on fixed pre-Task17 `0272922` (0/2, getter `17 !== 12`, second refresh `192.8859ms > 30ms`), so they are not Task17 production regressions. Their test oracles now distinguish an untrusted accessor/shallow freeze from a plain-JSON privately sealed immutable revision. Focused corrected test command (`--test-name-pattern='an accessor in a shallow-frozen world|one immutable large-world revision'`) → **2/2**, exit 0, retaining the 30ms sealed-world assertion.
- `git diff --check` → exit 0 (line-ending notices only).

## Remaining cost / limits

The existing flat SHA-256 commitment still requires a full changed-world hash on the acknowledgement path; this patch neither changes receipt bytes nor establishes the specification's no-global-hash target. Initial or invalidated archive validation remains full, and public complete boundary materialization iterates archived metadata. Program/Transform/Explore and other revision checks retain their own work. The content-free counters prove only a reduced private boundary parse for an unchanged archive, not end-to-end latency acceptance. Root's immutable-revision real-copy measurement and independent review remain the next gates.
