# Task 19 report — privately proven Atom field descriptions

Base: `82edf5ff87740f026d299666669f6dfed3163fdb`. Scope: `world-revision.mjs`, `query-capability.mjs`, directly affected tests. Backup environment was empty for every test command. No disk format, revision bytes, archive query scope, Program authorization order, or persistence code changed.

## Implementation and proof boundary

- The existing strict plain-JSON seal traversal now associates each validated object with a private pending token. Only a successful complete world seal plus exact revision computation activates that token. A failed seal cannot make a partially frozen object cacheable; external `Object.freeze`, getters, proxies, and caller-supplied flags confer no provenance. The token and field cache use weak identity ownership.
- Query field descriptions are prepared once for a proven Atom identity. Direct `walkAtoms`, Explore matching/indexing, access checks, and descriptions consume private frozen descriptors. Public `fieldsByBase` and `oneStoredField` create detached mutable Maps, lists, wrappers, and nested parsed metadata; they do not clone whole fact values. Mutable COW Atoms are re-described until their candidate is sealed, while unchanged archived objects remain reusable. Duplicate base fields and invalid keys retain the previous behavior.
- Explore and slot snapshot cache gates now require a privately sealed world, not a shallow-frozen root. The old Explore test's `Object.freeze`-only cache oracle was corrected to seal its world first; the once-sealed identity assertion remains. No parent/path, authorization decision, or match result is inherited merely from an Atom field description.

## RED → GREEN and exact verification

- RED: `$env:ATOM_RUNTIME_BACKUP_REPO=''; node --test --test-isolation=none tests/atom-sealed-field-descriptions.test.mjs` → 0/2. Repeated sealed archive `Object.entries` was 2 rather than 0; changing a shallow-frozen getter left a stale Explore path (`Before` rather than `After`).
- First GREEN: same command → 2/2. Final focused affected chain: `$env:ATOM_RUNTIME_BACKUP_REPO=''; node --test --test-isolation=none tests/atom-sealed-field-descriptions.test.mjs tests/atom-world-revision.test.mjs tests/atom-default-backup-active-boundary.test.mjs tests/atom-language-explore-full-path.test.mjs tests/atom-access-engine.test.mjs` → 44/44, exit 0. This covers typed default backup, equal joined paths with different ancestry, restore, exact ambiguity, access/denial and public-view isolation. An initial non-escalated run had only `EPERM mkdtemp` fixture failures; the permitted rerun passed.
- Existing real memory Explore→ordinary Transform→Explore / Program / old-reader test: `$env:ATOM_RUNTIME_BACKUP_REPO=''; node --test --test-isolation=none --test-name-pattern='real engine reuses an accepted memory version and preserves old readers through save' tests/atom-memory-authoritative-interaction.test.mjs` → 1/1, exit 0; retained fixture `C:\Users\worker\AppData\Local\Temp\atom-owned-real-memory-6AXH19`. Exact unchanged archived Atom `Object.entries` calls by request phase: first Explore 9, second Explore 0, Transform 8, post-write Explore 5. Direct `walkAtoms` on the old and new sealed versions after the journey added 0 each. The whole-request residual 8/5 is real and is **not** claimed to be field-description work or zero archive traversal. An initially over-broad `post-write Explore == 0` assertion failed at 5, then was narrowed to the field-preparation contract; no product code was changed in response.
- Existing Program/Agent/postcommit selection: `$env:ATOM_RUNTIME_BACKUP_REPO=''; node --test --test-isolation=none --test-name-pattern='ordinary situation Transform|Agent key, lock|Program structural facts|changing one referenced Program|ordinary transform trigger|onCommitted exposes' tests/atom-program-projection-lifecycle.test.mjs tests/atom-transform-postcommit-boundary.test.mjs` → 6/6, exit 0. Critical public rename/Agent journeys: `$env:ATOM_RUNTIME_BACKUP_REPO=''; node --test --test-isolation=none --test-name-pattern='rename-copy acceptance|public ordinary Agent keeps source success' tests/atom-transform-postcommit-boundary.test.mjs` → 2/2, exit 0. `git diff --check` → exit 0.

The first attempt to add the existing real-journey instrumentation test was auto-review rejected with backend HTTP 403. After root verified the single tracked test target, authorized scope, and absence of destructive/external effects, the exact same patch was retried once and accepted. No alternate path/tool bypass was used.

## Residual cost and acceptance

This changes repeated Atom key parsing/description, not full-world Atom visiting, non-query validation, Program settlement, or the exact flat SHA-256 of `JSON.stringify(facts)`. The latter still requires a changed world's digest before ACK under the current revision contract. The post-write Explore's 5 archived `Object.entries` calls demonstrate residual full-request work. No real-copy latency claim is made here; root owns the immutable real-copy comparison and the separately specified active/archive query-view boundary if seconds-long interactions remain.

## Fix round 1 — Transform relation-index provenance

Independent review found that `prepareTransformRelationIndex` used array identity as its cache authority, and `applyTransform` could write inherited matches/bindings for an unsealed COW result. This differed from Task19's private seal contract: a shallow-frozen getter, ordinary mutable edit, or an unsealed preparation followed by a seal could return stale paths or bindings. The minimal repair gates all relation-index cache reads/builds/writes on `isSealedWorldFacts`; unsealed worlds always recompute and never install a future sealed entry. The inherited-result write is likewise gated, so ordinary unsealed Transform results cannot seed the cache. Trusted sealed same-version identity reuse remains.

- RED: `$env:ATOM_RUNTIME_BACKUP_REPO=''; node --test --test-isolation=none tests/atom-transform-relation-cache-proof.test.mjs` → 0/4, exit 1. Failures were stale `Before` instead of `After` for a shallow-frozen getter and mutable facts, reference-equal unsealed→sealed prepared index, and stale `Renamed` instead of `After` after an unsealed structural Transform result changed.
- GREEN: same focused command → 4/4, exit 0. Directly affected `tests/atom-language-transform-p2.test.mjs` → 10/10, exit 0 (rename, move, discard/restore, copy, Strut identity and binding semantics). `--test-name-pattern='one accepted version retains the context and relation index across adapter reads' tests/atom-world-service-contract.test.mjs` → 1/1, exit 0. `--test-name-pattern='batch Transform moves multiple|trusted maintenance atomically moves|batch Transform swaps sibling names|batch rename rewrites Program' tests/atom-language-transform-batch.test.mjs` → 4/4, exit 0. Every command used `$env:ATOM_RUNTIME_BACKUP_REPO=''`; no full suite or real-copy test was run in this fix round.

The sealed-world full relation-index build still walks the whole world once for a new version. This fix removes false reuse, not that initial build or the flat revision hash.
