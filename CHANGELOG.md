# Changelog

- 2026-08-03: 内包（S）模式下新增 PageUp/PageDown：以光标所指最里层子域团为对象，单层收缩／展开；A/D/F 模式暂不接入。
- 2026-07-24: 关系起终点选定后立即显示虚线预览，Enter 后才形成正式实线关系。
- 2026-07-24: 团内新节点保留人工点击落点并随知识库持久化，不再因自动居中抹平批量建模位置。
- 2026-07-24: 中键按住旋转时才采用最近交互节点为视角中心；普通编辑、右键浏览与 Z/X 不移动相机。
- 2026-07-24: CapsLock 在镜面详情和名称下方透明页之间切换；内包团改为针尖细边与 15% 内部场强。
- 2026-07-23: 导入／导出入口新增 graph-4d JSON，并完整保留 Mermaid；两种格式共用选母球、顶层新增、原子预览和精确更新规则。
- 2026-07-23: P 演示新增可见木杖／玉杖轨迹与命中辉光；取景按当前完整空间内容自适应，离屏节点或域团也纳入边界；异常中断遗留的时间戳演示数据会在下次导入或演示启动时自动清理。
- 2026-07-23: P 演示改为按当前图自适应编排主题章节；支持时间戳演示节点、详情与关系，并在人工接管或循环结束时恢复原知识数据。
- 2026-07-23: 新增 P 空闲演示、持久化 P/H 启动设置与中央操作提示；普通右键浏览和 Z/X 历史不再改写滚轮缩放或 Space 相机。
- 2026-07-23: 修复沉浸子域中右键单击空白无法返回直接母域；空白无域壳命中时改用当前活动域。进入动画尚未结束时按 Z 也会准确回到进入前视角。

## 2026-07-23 · 肌肉记忆输入范式

- ASDF 直接切换外围、内包、层级、沉浸视角，只影响后续右键操作。
- 左键只发出一至三级使用信号；右键空白返回光标所在子域的直接母节点。
- Space 接管摄像机旋转与 Graph 平移，普通空白拖动不再改变视角。
- Shift 三击切换玉杖递归；木杖支持开放、闭环和五角星域内批量手势。
- 中键循环名称、球面详情、悬浮详情三态；关系线可直接修改名称。
- 解绑 CapsLock、M 星环和旧 B/V/E/R/C/L 等快捷键，Z/X 统一视图前后跳。

本项目的正式版本变化记录在此文件中。版本号遵循语义化版本，稳定快照同时发布为 GitHub Release。

## [0.3.0] - Unreleased

### Added

- 网页 Mermaid 导入／导出入口，支持选定母球、顶层警告、变更预览、文件读取、复制与下载。
- Mermaid 嵌套 `subgraph`、节点详情和关系解析，以及可再次导入的确定性导出。
- `spatial probe` 以关键词、方向和步数探查递归球域；默认返回域径、详情和子节点数。
- 上下、垂直、前后、平层与全向遍历，平层次序使用稳定的母域节点顺序。
- 跨进程共享的十秒滑动查询预算，同时累计节点数与实际输出字符数。
- 超限查询的一次性 `y/n` 确认协议，以及每个方向／步数的预计开销。
- CapsLock 多域球团全景：沿真实域径同时显示多个半透明域团、团内节点与跨域通道。
- 球团全景内继续进入、退出、旋转、选择祖先节点和切换球镜，并可无损恢复当前单域沉浸视野。

### Changed

- Mermaid 更新只在同母域按可见名称精确匹配；仅替换涉及节点自身详情，并保留既有子域与未涉及知识。
- 普通 Mermaid 节点缺少详情时整批拒绝，避免隐式补全语义。
- 所有 CLI 只读知识查询统一经过预算闸门，不能通过旧命令绕开累计限制。
- 文本搜索默认返回全部匹配项，仅在调用者显式给出 `--limit` 时收窄。
- CLI 应用层可在不派生子进程的情况下进行完整自动化测试。
- 多球团只改变视觉投影和相机，不写入或改写知识库数据。

## [0.2.0] - 2026-07-21

### Added

- 无中心多球域、三维环绕缩放、递归进入／退出与真实域径图。
- 节点和关系的增删改查、空间搜索、球镜详情与附件缩略展示。
- 跨域关系、空白落脚、历史长尾、入口球路由与微型扎入漩涡。
- 本地优先知识存储、浏览器桥和全局 `spatial` CLI。
- GitHub 原生贡献文档、架构决策、Issue／PR 模板与 CI。

### Changed

- 所有载体统一为可进入的隧洞球，取消不可扩展的实体终点。
- 球镜状态纳入跨域投影节点的域级切换、快照与持久化。
- 本机知识库运行数据退出 Git 跟踪。

### Verified

- Node.js 22 自动化测试全部通过。
- Windows 本地服务与真实浏览器交互完成验收。

[0.2.0]: https://github.com/open-worker-2077/atom/releases/tag/v0.2.0

